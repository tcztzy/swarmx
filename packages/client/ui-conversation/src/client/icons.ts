/** Single semantic mapping for SwarmX-owned conversation action icons. */
import { IconEditOutline16, IconRefreshOutline16 } from "@deepseek-ai/dsh-client-ui-primitives";

/** Replace a value here to customize artwork without touching action behavior. */
export const conversationIcons = {
  edit: IconEditOutline16,
  retry: IconRefreshOutline16,
} as const;
