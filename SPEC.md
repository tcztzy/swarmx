# SPEC

## §G GOAL

SwarmX presents the existing DeepSeek Harness Web application as its single safe local Electron UI and plugin host. DeepSeek Harness and Codex App Server are peer conversation runtimes below one narrow platform-neutral conversation boundary; each runtime remains the sole owner of its native transcript, model loop, tool execution, and approvals, while Science, PKB, and Swarm remain shared product capabilities.

## §C CONSTRAINTS

- DSH Web owns the only browser origin, shell, Conversation, Science, PKB, Swarm, settings, and plugin inventory UI. SwarmX starts no second conversation server or renderer.
- The selected native runtime owns threads, model access, native persistence, tool execution, and approval requests; SwarmX owns one disposable runtime-neutral projection, orchestration contract, Electron lifecycle, and product capabilities.
- Only bottom-level `dsh` and `codex` adapters may import platform SDK/wire types. Conversation orchestration, Retry/Edit/Fork policy, workspace authorization, Science, PKB, Swarm policy, and renderer state import neither platform.
- Renderer remains the sandboxed DSH Web profile with no preload or Node integration.
- The DSH Web server binds `127.0.0.1` on an OS-assigned port; the conversation-runtime Cordis plugin reuses that listener and never serves another `/`.
- Workspace uses `pnpm@11.7.0`; build tooling follows DSH TypeScript 6 + tsdown client conventions.
- The selected DSH session or Codex thread remains agent-interaction truth; the neutral projection is disposable and deterministically rebuilt. Science Journal owns scientific domain facts and never replaces conversation or activity projections.
- Science defaults to local-only execution/storage with no implicit network capability.
- Git and DVC integrations are optional Host capabilities over user-installed CLIs; desktop boot never installs, initializes, synchronizes, or contacts a remote implicitly.
- SwarmX PKB uses owner-readable local plaintext Markdown so Obsidian can open it directly; no implicit network, publication, sync, or at-rest encryption claim.
- PKB Wiki synthesizes durable personal knowledge; DSH session logs remain immutable conversation evidence and Science Journal remains scientific-domain truth.
- WikiSkill evolution reuses DSH native Session logs as Raw, keeps PKB's current OKF Vault as Wiki, and owns only staged/active `SKILL.md` + `PURPOSE.md` artifacts; it creates no second transcript or parallel Wiki store.
- WikiSkill proposal/evaluation is explicit and local-only; no background LLM, automatic PKB mutation, unvalidated cross-model activation, or candidate visibility in ordinary DSH skill catalogs.
- Swarm members share one checkout; read-only tasks may overlap, while SwarmX admits at most one active workspace-mutating attempt per Team.
- SwarmX-owned PKB and orchestration state stays owner-only under `$SWARMX_HOME`; an idempotent one-time import preserves existing `$DSH_HOME` product data, after which only the new location is authoritative. No workspace file or second conversation transcript becomes authoritative.
- Heterogeneous Swarm routing is measurable capability, not claim of lower cost or higher quality; local-model wall time/compute remains economic input.

## §I INTERFACES

- cmd: `pnpm dev` → delegate to `pnpm start`, build current source, start the selected runtime, and open one Electron window.
- cmd: `pnpm start -- --runtime <dsh|codex>` → build, always boot the DSH Web UI/Host, select the default peer conversation runtime (`dsh` by default), and open one Electron window; `SWARMX_RUNTIME` is the non-CLI equivalent.
- env: `SWARMX_CODEX_PAGINATED_HISTORY=1` → explicitly opt new Codex threads into experimental paginated history and latest-turn `thread/revert`; absent/`0` keeps legacy branch revision, and any other value fails before process launch.
- cmd: `pnpm build` → emit the DSH Web desktop host, runtime adapters, Host protocol plugin, and existing DSH client-plugin artifacts.
- api: `ConversationRuntime.list/create/read/start/steer/interrupt/revise/fork/archive/subscribe/respondToApproval/dispose` → the only native conversation lifecycle boundary; ids remain opaque and are qualified by runtime kind outside their adapter.
- service: `ctx.conversationRuntimes` → ordinary Host Cordis registry for peer conversation adapters and the only owner of their DSH-Web browser protocol.
- api: `ConversationController.retry/edit/fork` → runtime-neutral orchestration over native adapter `revise|fork`; Retry submits the exact original user input, Edit submits an explicit replacement only after composer confirmation, and explicit Fork always branches.
- type: `WorkspaceScope` → Host-minted canonical workspace root and opaque authorization token used by Science, PKB, Swarm, and model tools; native thread/session ids grant no file authority.
- endpoint: DSH Web `/api/swarmx/conversation-runtimes/*` → strict same-origin request/response and ordered SSE boundary registered through `ctx.webServer`; accepts no caller-selected host path and serves no HTML/assets.
- adapter: `apps/desktop/src/runtime/dsh/*` → maps the neutral contract to published DSH session/query/agent APIs and contains every desktop DSH import.
- adapter: `apps/desktop/src/runtime/codex/*` → maps the same contract to `codex app-server` stdio JSONL methods/events and contains every Codex wire type plus the allowlisted experimental paginated-history/revert and exact fork-boundary capabilities.
- process: `codex app-server` → one initialized child connection owned by the Codex adapter; stored threads resume before new turns, ordinary lifecycle otherwise uses stable thread/turn methods, exact native branches use `thread/fork.beforeTurnId`, explicit experimental configuration selects paginated creation, and paginated last-turn revision uses `thread/revert` followed by `turn/start`.
- process: SwarmX MCP stdio server → exposes the same workspace-authorized Science/PKB/Swarm tool handlers to Codex through configured MCP; DSH tool registration is a thin carrier over those handlers.
- file: `$SWARMX_HOME` → owner-only product state root independent of the selected runtime.
- file: `$DSH_HOME/profiles/web/cordis.patch.yml` → user-owned profile overrides.
- package: profile dependencies installed by `dsh plugin --profile web add` → out-of-tree DSH bundles resolved from the profile before the app-owned fallback graph.
- package: `@swarmx/dsh-ui-conversation` → product conversation presentation inside the sole DSH Web UI; runtime lifecycle reaches it only through the neutral Host protocol, while DSH-only Session extensions remain explicitly DSH-scoped.
- package: `@swarmx/annotation` → provider-neutral annotation contract; current OpenAI Responses output-text annotations are an unmodified subset and SwarmX adds user-comment targets without changing provider objects.
- api: `ctx.sideView.open/activate/close/dismiss/getSnapshot/subscribe` → generic per-Session serializable right-column tabs over published layout geometry.
- slot: `side-view.content` → keyed Session content renderer; Side View service stores descriptors only, never React nodes.
- package: `@swarmx/dsh-science` → `ctx.science`, client-safe contracts, SQLite Science Journal, projections, and strict Remote descriptors.
- preset: `dsh-science` → system-owned Science mode with the complete locked `standard` agent composition plus preset-scoped Science tools, annotation/literature guidance, and managed-Typst command protection.
- package: `@swarmx/dsh-ui-science` → additive Chat artifact cards plus a Claude Science-style `details` renderer; it registers no peer `conversation.view` beside Chat or Trajectory and exposes no direct execution control.
- api: `ctx.science.createProject/createNotebook/getWorkspace` → idempotent, abortable project/notebook vertical slice.
- api: `await ctx.science.registerArtifact(sessionId, request, signal?)` → workspace-relative generic file capture; resolves immutable metadata only.
- config: `embedArtifactMetadata` + request `reproducibilityMetadata` → default-on Figure PNG/SVG/PDF reproducibility metadata with global and per-artifact opt-out.
- api: `await extractArtifactMetadata(bytes, mime)` + format extractors → bounded validation + portable typed recovery of one embedded Figure generation record.
- api: `ctx.science.importArtifact(sessionId, request, signal?)` → bounded browser-file admission into the immutable Artifact Registry; infers trusted type from one logical filename and returns metadata only.
- api: `ctx.science.getResearchObject(sessionId, {projectId}, signal?)` → one project-scoped RO-Crate 1.3 JSON-LD Metadata Document whose flat `@graph` describes research entities and Schema.org provenance Actions without host paths or Session identities.
- api: `ctx.science.executeNotebookCell(sessionId, request, signal?)` → run one non-interactive Python cell in the live workspace and return bounded output, runtime metadata, notebook revision, and optional captured artifact.
- api: `ctx.science.previewArtifact(sessionId, request, signal?)` → return one authorized, digest-verified, bounded text/image preview or an explicit unavailable reason without revealing an artifact path.
- api: `ctx.science.headResource/batchHeadResources/getResource/selectResource/getResourceNeighbors` → resolve canonical `sx:<kind-prefix>/<encoded-id>[@revision]` addresses inside the live Session workspace and return strict bounded refs, metadata, preview windows, or neighbors.
- reference: `annotation` → serialize one bounded provider or SwarmX annotation into the DSH composer's detached-reference pipeline; the composer renders an interactive aggregate tray, the user transcript renders cards instead of protocol markup, and the model resolves protected image bytes only through `science_query.inspect_annotation`.
- reference: `message_quote` → serialize one bounded selection from a durable user, steering, or finalized assistant message into the same annotation pipeline; the source Session locator is distinct from the destination composer Session so later cross-Session insertion requires no protocol migration.
- reference: `[filename](./workspace-relative/path)` → preferred finalized-assistant file reference; mutation evidence plus safe workspace-relative Typst references in finalized Assistant prose or Tool arguments resolve through one product-owned opener, and Typst destinations enter the Host-authorized paper workbench.
- api: `ctx.science.previewTypstDocument/updateTypstSource/resolveTypstSourceAtPoint(sessionId, request, signal?)` → authorize one workspace-relative `.typ`/`.typst` paper, keep one managed local semantic Typst watcher, return bounded source/PDF revisions or exact compilation diagnostics with optimistic source writes, and resolve an exact rendered PDF point to one workspace-relative source caret.
- api: `ctx.science.createDocument(sessionId, request, signal?)` → create one bounded logical `.typ`/`.tex`/`.md`/`.bib` source document with revision metadata and diagnostics.
- api: `ctx.science.modifyDocument(sessionId, request, signal?)` → propose or accept/reject one source-selection patch against an exact document revision.
- api: `ctx.science.createFigure(sessionId, request, signal?)` → create one bounded matplotlib/seaborn/ggplot2/plotly code figure with a semantic object map and optional registered figure artifact.
- api: `ctx.science.modifyFigureCode(sessionId, request, signal?)` → propose or accept/reject one semantic-object-linked code patch against an exact figure revision.
- api: `ctx.science.createQuestion/createHypothesis/recordClaim/linkEvidence` → append bounded operational facts that project to Schema.org `Question|Claim|CreativeWork|Review|Rating` entities inside the project RO-Crate; Journal/SQL relation rows remain private projections.
- api: `ctx.science.defineExperiment/startRun/finishRun/compareRuns` → own an exact-revision experiment/run ledger with redacted reproducibility metadata and bounded metric comparison.
- api: `ctx.science.exportProject(sessionId, request, signal?)` → create one deterministic UTF-8 `ro-crate-metadata.json` conforming to RO-Crate 1.3, store it content-addressed, and append only bounded export metadata to Journal.
- api: `ctx.science.searchLiterature(sessionId, request, signal?)` → search the running local Zotero library, normalize every candidate through one owner-only BibTeX snapshot, and return bounded citation-ready records without reading attachment paths or contacting a cloud service.
- plugin: `@swarmx/dsh-science/tools` → the `dsh-science` preset's bounded aggregate `science_*` tools plus one explicit `literature_search` tool registered through the preset-scoped `ctx.tools` layer, each returning a fact/inference/proposal classification and Science entity locator where applicable.
- api: `JupyMcpRuntime.execute/readNotebook/close` → Host-only persistent Notebook Controller keyed by workspace + Notebook, backed by one local JupyMCP MCP stdio session through the official TypeScript SDK; returns bounded standard MIME outputs and owns MCP/kernel teardown.
- package: `@swarmx/dsh-ui-git` → read-only, Session-scoped Git status Remote plus the single browser Version Control header action and Details view that composes Git with optional DVC project status; registers no model tool and exposes no mutation.
- api: `ctx.gitUi.snapshot(sessionId, signal?)` → committed-HEAD identity, branch/upstream facts, and bounded porcelain-v2 staged/unstaged/untracked/conflict entries using repository-relative paths only.
- package: `@swarmx/dsh-dvc` → Host-only `ctx.dvc` service over an existing DVC project; owns path-free status, explicit live-workspace pull, and isolated clean-HEAD reproduction with a package-private Git/worktree runtime.
- api: `ctx.dvc.inspect(cwd, signal?)` → DVC/Git versions, Git HEAD, DVC project-relative root, SHA256 `dvc.yaml|dvc.lock` identities, and bounded path-free data/pipeline status summaries without remote refresh.
- api: `ctx.dvc.pull(cwd, request, signal?)` → explicit target/remote cache synchronization into the addressed live workspace; no implicit invocation or approval bypass through a model surface.
- api: `ctx.dvc.reproduce(cwd, request, signal?)` → require a clean committed Git workspace, create one detached worktree, reuse only private local DVC configuration/cache, run the requested DVC targets, and return a disposable Host-only result handle without mutating the source workspace.
- package: `@swarmx/dsh-ui-dvc` → read-only, Session-scoped DVC status Remote consumed by the Version Control view; delegates inspection to `ctx.dvc`, registers no separate browser surface or model tool, and exposes no DVC mutation.
- api: `ctx.dvcUi.snapshot(sessionId, signal?)` → derive the live Session workspace, return one typed `project|not-project|unavailable` result, and expose only the path-free `ctx.dvc.inspect` projection.
- package: `@swarmx/dsh-pkb` → Host-only OKF v0.2 Personal Knowledge Base service, one aggregate model tool, bounded index context, and DSH conversation-source retrieval.
- file: `$SWARMX_HOME/pkb/vault` → one owner-only Obsidian-openable OKF v0.2 Markdown bundle; `index.md` and `log.md` plus global/workspace concepts and bounded conversation-source excerpts.
- api: `ctx.pkb.search/read/create/update/deprecate/searchConversations/readConversation/getVaultInfo` → workspace-authorized Wiki and cross-Session knowledge operations with exact revisions, cancellation, and bounded results.
- tool: `pkb` → aggregate `search|read|create|update|deprecate|search_conversations|read_conversation|vault_info` actions; no `memory` compatibility alias.
- package: `@swarmx/dsh-wikiskill` → DSH-raw/PKB-wiki/WikiSkill-skills evolution core plus exact-Agent active-skill provider registration; no model Tool or duplicate skill loader.
- file: `$SWARMX_HOME/skills` → owner-only `staging/<proposal-id>` candidates and `active/<target-key>/<skill>/{SKILL.md,PURPOSE.md}`; only exact-target active roots are discoverable by DSH.
- api: `DshRawTraceReader.read` → bounded exact DSH Session event window for Maintainer/Proposer use without persisting a Raw copy.
- api: `WikiSkillStore.stage/resolve/readProposal/readActive/activeRoot` → single-skill proposal, strict target validation gate, revision-fenced promotion/rollback, and PKB-ready `SkillImpact` result.
- adapter: `registerWikiSkillProvider(agentCtx, store)` → agent-scoped `@deepseek-ai/dsh-skill-filesystem` provider over only the active root matching the live DSH Agent's exact preset/model; existing DSH catalog/loader remains sole runtime consumer.
- package: `@swarmx/dsh-swarm` → Host-owned Team roster, mailbox, task DAG, event journal, continuable-member lifecycle, scheduler, aggregate model tool, and strict UI Remote.
- file: `$SWARMX_HOME/swarm/swarm.sqlite` → owner-only SQLite WAL event log plus replayable materialized Team projections; workspace contains no authoritative swarm state.
- api: `ctx.swarm.create/addMember/sendMessage/createTask/updateTask/submitTask/startVerification/recordVerdict/escalateTask/reassignTask/interruptMember/admitKnowledge/resolveEffect/waitForChange/archive/snapshot` → exact-Agent-authorized R/W/K orchestration, role/model routing, attempt budgets/economics, independent verification, Tool-effect reconciliation, evidence admission, and Session-scoped read projection.
- tool: `swarm` → aggregate `create|status|add_member|send_message|create_task|update_task|submit_task|start_verification|record_verdict|escalate_task|reassign_task|interrupt_member|admit_knowledge|resolve_effect|wait|archive` actions, visible only in `dsh-swarm`.
- preset: `dsh-swarm` → system-owned Team mode with the complete locked `dsh-science` composition plus preset-scoped `swarm` tool and Team guidance.
- package: `@swarmx/dsh-ui-swarm` → strict Session-scoped `swarm/uiSnapshot|waitUi` Remote, one conversation header action, one keyed Side View activity renderer, and bounded historical Tool card.

## §R RESEARCH

id|topic|finding|src
R17|OKF v0.2|bundle = UTF-8 Markdown tree + YAML frontmatter; concept `type` required; `index.md`/`log.md` reserved; unknown fields tolerated|https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md
R18|OKF provenance|`sources[].resource` records provenance; stable `sources[].id` joins per-claim Markdown footnotes; standard relative links supported|https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md
R19|Obsidian interop|YAML properties + standard Markdown links supported; Wikilinks optional; block references Obsidian-specific|https://help.obsidian.md/properties
R20|MyST interop|YAML frontmatter, relative Markdown document links, named footnotes, and standard citations supported|https://mystmd.org/guide/quickstart-myst-markdown
R21|DSH conversation query|`ctx.sessionQuery` owns exact/filter/search/read-window APIs; literal text filter remains provider-independent; callers own authorization|https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/session-query/session-query/README.md
R22|Git machine status|`git status --porcelain=v2 -z --branch` is config-stable, NUL-delimits raw paths, emits extensible `#` headers, and requires parsers to ignore unknown headers|https://git-scm.com/docs/git-status
R23|Git isolated worktree|`git worktree add --detach <path> <commit>` creates a branchless linked worktree; normal lifecycle ends through `git worktree remove`, with `--force` required for dirty disposable trees|https://git-scm.com/docs/git-worktree
R24|DVC status|`dvc data status --json` is read-only workspace-vs-HEAD data status; `dvc status --json` reports pipeline/cache state, and remote comparison occurs only when explicitly requested|https://doc.dvc.org/command-reference/data/status
R25|DVC reproduction|`dvc repro` executes required dependency-graph stages and deletes non-persistent stage outputs before their producing command; `--pull` is explicit|https://doc.dvc.org/command-reference/repro
R26|DVC synchronization|`dvc pull` downloads selected tracked objects into cache then links/copies them into the workspace; remote selection is explicit or project-configured|https://doc.dvc.org/command-reference/pull
R27|DSH Agent Teams reference|community implementation proves continuable members, attempt-fenced DAG scheduling, durable mailboxes, event-driven status handling, and Tool-derived UI; its workspace JSON/JSONL truth and process-global service wrapping are unsuitable for SwarmX|https://github.com/NanmiCoder/dsh-agent-teams/tree/912aae5225d3d85fa841a1b0c8a5c77021876c25
R28|DSH experimental Agent Teams|official private workspace design uses root Session identity, exact Agent authority, provisioning-before-spawn, bounded mailbox/task state, revision CAS, lifecycle drain, and wait-for-change; it is a design oracle, not a publishable dependency|https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/experimental/agent-team/README.zh.md
R29|DSH continuable setup|`ctx.subagents.registerContinuableSetup` composes and immediately revokes child-scoped capabilities; `agent.ctx.tools.guard|restrict` enforce execution and visibility per exact Agent without monkey-patching process-global services|https://github.com/deepseek-ai/deepseek-harness/tree/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/subagent/subagent
R30|shared-tree concurrency|CoAgent already defines concurrency control for multiple coding Agents sharing a Git tree; SwarmX's claim is the DSH attempt/evidence-boundary composition, not first concurrency control|https://arxiv.org/abs/2606.15376
R31|stateful effect governance|policy-state serializability already requires consequential effects to be authorized against current policy state rather than stale request context|https://arxiv.org/abs/2608.02764
R32|recovery and non-atomic calls|AgentRewind covers recoverable long-horizon execution; Verified Tool Calls covers postcondition verification, verify-before-retry, and idempotency under uncertain non-atomic outcomes|https://arxiv.org/abs/2608.14380;https://arxiv.org/abs/2608.02645
R33|agent provenance|PROV-AGENT and the 2026 evidence-tracing/execution-provenance survey cover agent-centric lineage; an orchestration log alone does not admit a scientific fact or durable personal synthesis|https://arxiv.org/abs/2508.02866;https://arxiv.org/abs/2606.04990
R34|Codex App Server|App Server is the supported integration surface for authentication, conversation history, approvals, and streamed agent events; default transport is bidirectional JSON-RPC as JSONL over stdio, while WebSocket transport is experimental|https://developers.openai.com/codex/app-server
R35|Codex threads and turns|stable v2 methods include thread start/resume/read/list/fork and turn start/steer/interrupt; a client initializes once, then rebuilds UI from native thread items plus ordered notifications|https://developers.openai.com/codex/app-server
R36|Codex approvals|command execution, file changes, permissions, MCP elicitation, and tool user-input arrive as server requests that the client must answer; approval policy is selected per turn and unresolved requests must not be silently accepted|https://developers.openai.com/codex/app-server
R37|Codex MCP|Codex consumes configured MCP servers as its stable external tool extension surface; MCP carries SwarmX product tools but does not own conversation lifecycle, and experimental dynamic tool injection remains unnecessary|https://developers.openai.com/codex/mcp
R38|WikiSkill method|WikiSkill separates immutable Raw traces, persistent Wiki patterns/impact, and `SKILL.md` + `PURPOSE.md`; each iteration proposes one skill change, validates against the current baseline, promotes only improvement, rolls back the skill otherwise, and retains Wiki learning|https://arxiv.org/html/2608.27454
R39|WikiSkill transfer risk|WikiSkill reports large negative cross-model transfer and requires target-model validation; its ablation also favors Wiki access for Maintainer/Proposer rather than the ordinary Inference Agent|https://arxiv.org/html/2608.27454

## §V INVARIANTS

V1: ∀ desktop boot → Harness browser handoff disabled; exactly one SwarmX-owned surface opens.
V2: ∀ renderer navigation → same Harness origin stays in-window; foreign `http:`/`https:` may open externally; every other protocol is denied.
V3: ∀ renderer browser permission → denied unless a later documented capability adds a focused allow rule and regression test.
V4: ∀ generated build output → excluded from source lint; authored source remains formatted and checked.
V5: ∀ initial renderer load failure → error is reported and the window is shown instead of remaining invisibly blocked.
V6: ∀ re-run lookup → use real DSH conversation nodes and turn boundaries; missing or out-of-window evidence declines safely.
V7: ∀ native DSH Edit action → prepare a separate session before the selected turn, open it, seed its draft; source session remains unchanged.
V8: ∀ first-turn DSH Retry/Edit → alpha.2 `ctx.sessions.create` creates an immediately addressable session in the source Workspace (adopting its cwd when ungrouped) with the source agent preset, because DSH `session.fork` cannot represent an empty completed-turn prefix.
V9: ∀ closed user-originated message with a safe preparation strategy → Edit renders as an icon beside that user message; assistant action rows own no SwarmX Edit control.
V10: ∀ turn ending in error → visible failure message and Retry icon render even when no finalized assistant message exists.
V11: ∀ native DSH Edit/Retry → source log remains append-only and unchanged; active child starts before the superseded turn and appends only the revised/retried user input; superseded messages, `llm/retry`, and `turn/end(error)` remain in source history but stay outside child Chat/model projections and LLM prompt payloads.
V12: ∀ eligible user-message action row → the clock, Copy, and Edit render in that order; Copy and Edit each reserve their own 28px lane with the native 10px action gap, so neither icon overlaps the clock.
V13: ∀ SwarmX conversation action icon → its semantic component resolves through one local `conversationIcons` map; changing icon artwork requires no action/controller/layout or client-bundle configuration change.
V14: ∀ reusable package → `packages/<group>/<package>`; browser plugin at `packages/client/ui-conversation`; executable assembly at `apps/desktop`.
V15: ∀ clean root build → host/client TypeScript references + tsdown emit desktop `dist/` and plugin `lib/{index.js,client.js,types/**}`.
V16: ∀ shared build tool → root ownership; client bundle keeps DSH module-table identities external.
V17: ∀ Science UI mount → register no `conversation.view`; contribute only additive Chat turn-tail artifact cards, keyed Side View content, and annotation references while Chat snapshot, Trajectory snapshot, composer, and agent loop remain unchanged.
V18: ∀ Science mutation → validate client-safe input & honor pre-aborted `AbortSignal`; one idempotency key commits ≤1 journal event and projection update in one transaction.
V19: ∀ committed Science fact → append-only SQLite WAL journal is truth; versioned migrations and deterministic replay rebuild materialized views.
V20: ∀ Science artifact bytes → immutable content-addressed object outside journal; journal stores bounded metadata/reference only.
V21: ∀ Science default storage/execution → local-only, owner-only paths, no implicit network, no shell-concatenated command.
V22: ∀ Science plugin disposal/reload → close owned database/process resources and withdraw slot/Remote contributions; replacement mount reopens durable state without duplicate effects.
V23: ∀ Science project/document/figure/research/experiment operation → remain agent-mediated through Chat and the seven aggregate Science tools; no browser Science destination strip or duplicate workspace surface exists.
V24: ∀ Science Remote access → host derives an opaque workspace key from a live DSH session `cwd`, rejects cross-workspace entity ids, and returns no host path to UI.
V25: ∀ artifact registration → stream bytes through owner-only staging, compute SHA256, fsync, atomically publish one immutable object per digest, then append metadata; abort/failure before Journal commit may leave only an unreachable immutable object.
V26: ∀ artifact source → non-empty relative path resolves through realpath to a regular file inside live session `cwd` and within configured byte limit; reject absolute/traversal/symlink escape; Journal/Remote contain neither source path nor bytes.
V27: ∀ project Research Object read → derive one project-only RO-Crate Metadata Document from client-safe Journal projections, cap output through the existing Science bounds, and exclude host paths, Session identities, environment secrets, and model-private reasoning.
V28: ∀ client-supplied Science environment metadata → redact secret-key values and absolute paths before Journal commit or Remote return.
V29: ∀ explicit Isolated Run Python execution → resolve the configured executable through `ctx.subprocess`, pass source through stdin with explicit argv/cwd/stdio/env (no shell), bound both output streams, terminate on abort/disposal, and append no execution fact after cancellation.
V30: ∀ settled Notebook cell execution → one idempotency key atomically appends ≤1 `notebook/cell-executed` fact and updates notebook cells/revision plus at most one output artifact; replay restores both, including failed-execution evidence, ordered bounded outputs, and redacted path-free runtime metadata.
V31: ∀ one aggregate Science event touching multiple entities → emit at most one provenance Action for its Journal sequence and merge its standard `object|result` references.
V32: ∀ root `pnpm test` → rebuild current library/plugin artifacts before the real Harness packaging test boots package `main` entries.
V33: ∀ Science document create → accept one traversal-free logical name ending `.typ`/`.typst`/`.tex`/`.md`/`.bib`, bounded UTF-16 source, workspace-local project ownership, and one replayable `document/created` fact.
V34: ∀ document patch mutation → require exact `expectedRevision`; proposal selection uses textarea-compatible UTF-16 `[start,end)` offsets and snapshots original/proposed text plus reasoning; propose/reject preserve source, accept replaces only the snapshotted range, and each action appends ≤1 replayable fact.
V35: ∀ Writing diagnostics → deterministic bounded ranges/messages derived from stored source; distinguish structural checks from full Typst/LaTeX compilation and never report structural success as compile success.
V36: ∀ Figure create → accept one supported plotting library and bounded source; derive bounded axis/legend/annotation/line/point/image-layer/data-series objects with valid UTF-16 code ranges, verify any artifact is a workspace-local `figure`, and append one replayable `figure/created` fact.
V37: ∀ Figure patch mutation → require exact `expectedRevision` and ≥1 current semantic object; proposal snapshots selected object ids, their enclosing code range, original/proposed code, instruction, and proposal reasoning; propose/reject preserve code and accept replaces only that range in one replayable fact.
V38: ∀ accepted Figure code patch → preserve selected semantic object identity, shift unaffected later ranges by the UTF-16 delta, reject ambiguous overlap, and keep every object range inside the new code.
V39: ∀ operational research fact → belong to one workspace-local project, use a bounded question/hypothesis/claim/evidence/decision/review/open-question kind, append exactly one replayable Journal fact, reject cross-project/workspace endpoints, and project through standard Schema.org entities/properties rather than expose a parallel Research Map graph contract.
V40: ∀ Experiment/Run mutation → require workspace-local ownership and exact revisions; a Run starts once, finishes once into succeeded/failed/cancelled, records redacted bounded environment/metrics/artifact references, and replay restores the complete lifecycle.
V41: ∀ Run comparison → compare 2–10 completed Runs of one Experiment without mutation, return bounded finite numeric deltas against the first Run, and classify the result as inference rather than fact.
V42: ∀ project export → serialize only one workspace-local project's client-safe facts as deterministic UTF-8 RO-Crate 1.3 `ro-crate-metadata.json` with `application/ld+json`, store bytes as an immutable SHA256 object outside Journal, append only bounded digest/size/count metadata, and return an exact idempotent payload capped by configured export size.
V43: ∀ `dsh-science` agent surface → expose exactly `science_notebook`, `science_write`, `science_figure`, `science_experiment`, `science_record`, `science_query`, and `science_export`; validate strict JSON, forward `AbortSignal`, expose no SQL/path/private reasoning, and rely on the profile's session-scoped spill policy for oversized rendered results.
V44: ∀ successful Science tool result → include a durable locator containing session id, tool call id, entity kind/id, and Journal sequence plus a fact/inference/proposal classification; DSH tool events remain the trajectory association and no agent-loop or Trajectory snapshot mutation is permitted.
V45: ∀ Science UI Remote mutation rejection → enter the visible retryable error state exactly once and never run the mutation's success reload path.
V46: ∀ Science client registration → occur only inside a Cordis child context injected with `remote.science`, so every rendered Remote call has an active namespace service.
V47: ∀ `ctx.sideView.open(sessionId, entry)` → accept one JSON-serializable `inspect|workbench` descriptor, upsert by entry id inside that Session, activate it, and open published `ctx.layout` details geometry; service stores no React node or host path.
V48: ∀ Side View tab mutation → open/switch/close order and active fallback remain deterministic per Session; one Session cannot observe another's tabs; dismiss preserves tabs while HMR disposal clears owned state/listeners, closes details, and withdraws registrations/service.
V49: ∀ generic Side View mount → shadow only published root `details` at an explicit lower render priority, declare keyed `side-view.content`, redeclare no upstream-owned child slot, expose labelled tabs with wrapping Arrow/Home/End keyboard navigation, and never replace `conversation`, `conversation.session`, Chat snapshot, composer state, or chat scroll memory.
V50: ∀ Science artifact Side View entry → carry bounded client-safe artifact metadata/provenance only, route by `science-artifact`, deduplicate repeated opens into one active tab, and expose no artifact bytes or host path.
V51: ∀ Side View mode → details opens at 360px and remains draggable above its 300px floor without a fixed maximum; the concession solver derives the rendered maximum from the viewport after preserving the sidebar and a 640px center, while narrow concession may visually close details without mutating tabs; `workbench` remains an explicit descriptor with the fullscreen fallback.
V52: ∀ artifact preview → resolve artifact id only inside the requesting Session workspace, verify immutable bytes against the registered digest, return either bounded text (≤64 KiB), a bounded PNG/JPEG/GIF/WebP data URL (source ≤2 MiB), or an explicit unsupported/too-large result; support AbortSignal and expose no host path or network URL.
V53: ∀ conversation artifact-card activation → open or activate one deduplicated per-Session `science-artifact:<id>` DetailsPanel tab from the same bounded locator without changing the active Chat view, composer draft, or scroll memory.
V54: ∀ completed aggregate Science Tool result carrying a locator whose `entityKind` equals the parsed artifact's concrete `kind` → a bounded strict parse may contribute that artifact only to the same-Turn generated card group; malformed, cross-Session, mismatched-kind, and oversized results contribute no card.
V55: ∀ Side View width adaptation → the exact-baseline package-manager patch may remove only the installed release's fixed details ceiling; implementation must not directly edit installed dependencies, copy private DetailsPanel, redeclare its private child slot, or bypass the concession solver with CSS.
V56: ∀ Science Client surface → expose no Notebook destination, cell source/output, Notebook create/execute/analyze control, or Jupyter rendering dependency; Notebook creation and execution remain reachable through the aggregate `science_notebook` tool and Host-only service/runtime.
V57: ∀ completed Chat turn with one or more valid same-Session Science artifact tool results → render one `GENERATED · n` card group beneath the closing answer, preserve tool-result order with artifact-id deduplication, load image thumbnails through bounded authorized preview, and make every complete card one keyboard-operable DetailsPanel action.
V58: ∀ agent-requested Notebook output capture → empty path executes without artifact; non-empty path must be traversal-free workspace-relative with a supported deterministic kind/MIME, is sent in the same `executeNotebookCell` request, and on success appears once in Artifact Registry linked to that execution/Notebook; invalid capture input cannot reach JupyMCP.
V59: ∀ populated Artifact Registry card → render title and bounded kind/size metadata as one distinct readable identity block, keep provenance detail in Side View, and make the complete card the independently operable preview action.
V60: ∀ Science artifact DetailsPanel → reuse DSH AppFrame's existing right column and Side View tab strip, then render a compact filename/action bar, edge-to-edge large preview, optional Claude-style image annotation, on-demand Provenance, and collapsed file details; never add a nested Science shell or replace Chat, composer, Session navigation, or layout geometry.
V61: ∀ Notebook operation → the persistent agent composer and `science_notebook` tool are the only user-facing control path; return bounded execution evidence through the ordinary Tool/Chat surface and expose generated artifacts through the unchanged per-Session Side View locator.
V62: ∀ Science artifact Side View → prioritize the authorized file preview before compact provenance/details, keep filename and working More/fullscreen actions in a file header, and retain loading/error/unsupported states without exposing bytes, host paths, or network URLs.
V63: ∀ Chat with an open Science DetailsPanel → preserve the readable conversation column without horizontal overflow; the existing concession solver may shrink or visually close details when the viewport cannot retain the center minimum.
V64: ∀ bundle installed into the active profile through the published DSH plugin command → load its patch and module from that profile's `node_modules` before the app-owned `$DSH_HOME/profiles/node_modules` fallback; keep in-box and SwarmX product bundles resolvable without copying third-party source or editing dependency internals.
V65: ∀ details drag preference >520px → retain that preference and render it when the viewport can preserve `CENTER_MIN`; when it cannot, shrink or close details through the existing concession chain and restore the preference on re-widening.
V66: ∀ browser artifact import → accept one basename ending `.csv|.tsv|.json|.txt|.md|.xlsx|.pdf|.png|.jpg|.jpeg|.gif|.webp`, infer kind/MIME on Host, require canonical base64 decoding to 1–8 MiB, bind project to the requesting Session workspace, fingerprint idempotency with decoded SHA256, and persist/return neither encoded bytes nor a host path.
V67: ∀ Notebook artifact input → accept 1–4 workspace-local artifact ids totaling ≤32 MiB, verify immutable bytes, materialize owner-only disposable copies, expose their paths only as `DSH_SCIENCE_INPUT_<n>` during controller-owned silent kernel setup, serialize only that setup→visible-cell critical section across a shared workspace peer, remove those variables and copies after success/failure/abort/disposal, and record the exact input ids on replayable cells without returning paths.
V68: ∀ Science artifact registration/import → remain agent-mediated and return an immutable artifact locator through the ordinary Tool/Chat flow; accepted artifacts become addressable by subsequent agent tools and eligible for the same conversation card/DetailsPanel path.
V69: ∀ Chat-requested analysis of an imported file → the agent may select or create one Notebook, execute an inspection cell with that artifact as input, optionally capture a bounded artifact, and preserve Science UI, composer, scroll, Side View tabs, and agent loop.
V70: ∀ CSV/TSV or scalar-record JSON artifact preview → parse verified workspace-local bytes on Host into at most 500 rows × 256 typed columns, preserve duplicate/blank headings without object-key loss, report truncation, return scalar client-safe cells only, and render through AG Grid Community in Side View/workbench rather than raw text; cancellation, digest verification, path secrecy, and no-network defaults remain unchanged.
V71: ∀ Notebook state → canonical `.ipynb` cells and MIME outputs remain Host-side document truth, one persistent Jupyter controller owns kernel execution, and Science Journal adds provenance without introducing a browser editor or renderer.
V72: ∀ Jupyter kernel session → Host owns one controller per workspace + Notebook while one configured JupyMCP stdio server is shared only within that verified workspace through the official MCP SDK with DSH-scrubbed environment and workspace cwd; expose no MCP transport, kernel connection file/port/token/path to Client, perform no implicit `uvx` download, package install, remote-server connection, or outbound dependency fetch, and report explicit unavailable state when prerequisites fail.
V73: ∀ Jupyter cell execution → serialize requests per controller, call JupyMCP `execute` with a Host-derived workspace-relative `.ipynb` path, preserve kernel state across cells, normalize ordered MCP text/image/audio/embedded-resource blocks into bounded client-safe `stream|display_data|execute_result|error` outputs, classify cancellation before Journal mutation, and discard an aborted controller before later reuse.
V74: ∀ Jupyter controller abort/disposal → settle its queued/in-flight calls and shut down only its owned JupyMCP kernel before reuse; ∀ runtime disposal/reload → stop admitting calls, settle every controller, request `shutdown_all` once per workspace, close each official MCP Client/stdio transport exactly once, and leave no owned JupyMCP/kernel/input staging process behind; teardown failure is explicit and never reported as success.
V75: ∀ stateless Python execution → label and invoke only as explicit Isolated Run; never silently substitute it when persistent Jupyter is unavailable or present its fresh namespace as Notebook kernel semantics.
V76: ∀ JupyMCP controller → start one kernel per Notebook, retain its returned `kernel_id`, pass that id to visible execution and controller-owned silent setup, install one-shot post-cell cleanup for temporary input environment, and target that id for shutdown; multiple Notebook controllers may share one workspace MCP server but never rely on running-kernel list order or share kernel state.
V77: ∀ JupyMCP Notebook document → store only as the Host-derived owner-only `.dsh-science-notebook-<notebook-id>.ipynb` hidden file inside the verified workspace, so the authority-only `notebook://{path}` resource template can address it without unsupported slash encoding; bound resource reads independently from one-cell outputs and return neither its name/path nor raw unbounded document to Client.
V78: ∀ Notebook MIME output → preserve ordered bounded client-safe execution evidence for Tool/Journal consumers, but never mount it as Notebook cells or load JupyterLab/Lumino rendering code in the browser bundle.
V79: ∀ Science artifact Side View → remain an artifact preview by default; expose Provenance from the artifact actions menu, load the owning project's bounded RO-Crate only after selection, project the current artifact's standard lineage/Actions, and return to the unchanged preview without replacing the right column with a permanent provenance surface.
V80: ∀ image artifact annotation → derive one normalized point inside the rendered image, require a non-empty bounded comment, retain numbered annotations with edit/delete/reopen behavior in the active artifact entry, and provide a keyboard-operable center-point fallback; non-image and letterbox clicks cannot create annotations.
V81: ∀ saved image annotation → insert exactly one structured `annotation` occurrence through the public per-Session composer input/reference seam while preserving the existing draft; its `comment.target.type=image_point` payload carries only the verified artifact identity, normalized point, and comment; the `science_query` contract exposes every required annotation field so `inspect_annotation` cannot be inferred as an artifact-only request, then re-authorizes the artifact/digest before returning a durable image block plus point context to the model.
V82: ∀ Science artifact workbench open while details is closed → request an 880px preferred width matching the Claude Science split view at the 1920px QA viewport; inspect entries retain the 360px default, user drag remains authoritative while open, and viewport concession still preserves the 640px center floor.
V83: ∀ Science artifact header fullscreen action → enter the browser Fullscreen API for that artifact panel, remain dismissible through the platform Escape behavior, and never create a second route, window, or duplicate asset state.
V84: ∀ direct `@deepseek-ai/dsh*` dependency → use one coherent release baseline selected from authoritative npm publication timestamps rather than mutable dist-tags; any exact-version package-manager patch must target that same baseline, and the complete installed graph must resolve without an older DSH release line.
V85: ∀ injected Figure PNG/SVG/PDF metadata → one format-independent `dsh-science.figure-provenance` v1 record contains exact plotting code + SHA256, supported library, redacted runtime, generation id, and ≤32 normalized `workspace|artifact|s3` source references; workspace references ! traversal-free relative path + Host-verified SHA256, artifact references ! workspace-local id + immutable digest, S3 references ! credential-free `s3://bucket/key`; absolute paths ⊥.
V86: ∀ metadata capture → MIME and structure agree; PNG owns one `iTXt` keyword `dsh-science.provenance`, SVG owns one never-rendered `<metadata id="dsh-science.provenance">` foreign-namespace record, PDF owns one Catalog XMP `dsh:provenance` property; replace only prior owned data, preserve unrelated metadata values, hash/publish only transformed bytes through owner-only staging, preserve generator source bytes, cap decoded canonical JSON at 1 MiB, then commit Journal metadata; output digest/artifact id ⊥ embedded because self-reference cannot stabilize.
V87: `embedArtifactMetadata=false` | request `reproducibilityMetadata=false` → byte-identical ordinary capture with no owned data; default enabled + Notebook Figure PNG/SVG/PDF → exact executed cell source and input Artifact ids auto-captured; explicit registration supports matplotlib/seaborn/ggplot2/plotly through one generator-independent postprocessor.
V88: ∀ malformed/unsupported metadata target | duplicate/invalid owned record | non-UTF-8 SVG | encrypted/signed PDF | PDF/A lacking a valid custom extension schema → reject before Journal commit with no source mutation; extraction remains bounded and treats absent owned data as `undefined`, never executes SVG/script/XML/code, and no XML external entity or PDF attachment is created or resolved.
V89: ∀ Typst paper preview → require one traversal-free workspace-relative `.typ|.typst` regular file whose realpath remains inside the live Session cwd, resolve the bundled or explicitly configured writing preview runtime through `ctx.subprocess`, run one managed compiler/watcher per workspace + paper, produce PDF and inverse-search state from the same retained Typst document/world, keep all output in an owner-only disposable directory, cap source/PDF/diagnostics, expose no canonical path, and terminate every watcher on replacement, Session/plugin disposal, or failure.
V90: ∀ open Typst paper DetailsPanel → render one production PDF.js document viewer with selectable text layers and multi-page zoom, expose one right-header toggle between editable source and PDF without replacing Side View tabs, poll the Host revision while visible, retain the last successful PDF across a later compile error, and show explicit missing-compiler, compiling, stale, conflict, error, and retry states.
V91: ∀ Typst source write → require the exact current SHA256 source revision, use owner-only atomic replacement without following a changed symlink, preserve the original file on conflict/failure, and let the existing watcher produce the next PDF; the Client never claims a save or compile succeeded until its corresponding Remote response proves it.
V92: ∀ PDF text annotation → accept only a non-empty bounded selection wholly inside one rendered PDF.js text layer, capture its page, normalized bounding rectangle, exact selected text, owning relative paper path, and PDF/source revisions, then insert exactly one `annotation` with `comment.target.type=document_text` through the public per-Session composer reference seam while preserving the existing draft; serialization contains no PDF bytes, data URL, host path, DOM content, or executable markup.
V93: ∀ detected PDF figure region → a single point action creates the same bounded paper annotation with normalized figure-local coordinates; a double action opens one deduplicated Side View figure tab backed by that paper/page/region instead of a browser popup or duplicate file copy, where the existing image-point annotation path remains available for conversation-led image editing.
V94: ∀ produced-file row or settled inline-code file mention → use the dsh 0.1.2-alpha.2 `chatFileMentions.resolve` contract directly while preserving the mutation-derived path vocabulary, basename ambiguity rejection, ordinary Host opener, order/deduplication, accessibility, and folder action; only `.typ|.typst` is rerouted to the Session's Typst DetailsPanel.
V95: ∀ replacement conversation node definition → every published turn-location data key must exactly equal that definition's owned `kind`; the alpha.2 `uiConversation` assembler must accept the Typst-aware deliverables node without console errors during live event rebuilds.
V96: ∀ published DSH browser plugin → emit one self-contained `lib/client.js`; lazy third-party modules must be inlined, and the runtime bundle may not require unpublished relative chunks that the DSH module table cannot resolve.
V97: ∀ Typst watch output → normal progress and successful-compilation lines are controller state, not diagnostics; only actionable compiler diagnostics may drive error/stale UI, and a successful PDF revision clears prior compiler diagnostics.
V98: ∀ PDF text selection | figure-point annotation → open one floating annotation editor beside the pointer/selection anchor, prefer below and flip above when needed, clamp the complete editor inside the visible viewer/viewport boundary, and never place it relative to the full PDF document tail.
V99: ∀ completed Chat turn → the SwarmX turn-tail bridge renders only explicitly registered child contributions; it never derives or appends a generic `Inspect <tool>` action from Bash or any other Tool call.
V100: `OpenAIResponseAnnotation ⊆ Annotation`: ∀ current OpenAI Responses `file_citation|url_citation|container_file_citation|file_path` object → `Annotation` accepts it unchanged, preserves all official fields plus provider-added fields, and serializes it losslessly; SwarmX extensions use separate `comment` + discriminated `target` branches, never reinterpret or add required fields to an OpenAI object.
V101: ∀ annotation composer/transcript projection → composer keeps each occurrence as a detached reference and shows one aggregate count trigger plus an ordered editable/removable list; submission emits one hidden `<dsh-annotation>` payload per occurrence; user Chat renders bounded annotation cards plus safe Markdown body, Copy emits readable Markdown, and raw tag/JSON/model-only instructions never render. Persisted `<annotation>|science-image-annotation|science-paper-annotation` text receives projection-only recovery without history mutation.
V102: ∀ finalized assistant reference to a file produced in that turn → use the exact workspace-relative path as Markdown inline code; resolve only through the alpha.2 mutation-derived exact-path/unique-basename vocabulary, keep unresolved or ambiguous mentions inert, route `.typ|.typst` to the paper workbench, and mount no Markdown-link or prior-version compatibility path.
V103: ∀ `dsh-science` model-authored Typst workflow → source mutation is the final model operation; the preset-scoped Science prompt instructs no `typst compile|watch` tool call, a monotonic Bash guard denies a direct attempt, and only the Host-owned semantic paper-preview controller starts/reuses compilation and observes later source/import changes. A skill may teach authoring technique but never owns linking, compilation, preview activation, or inverse search.
V104: ∀ ordinary primary click inside one PDF.js text layer with no non-collapsed selection → send one bounded `page + normalized point + exact pdfRevision` query to the owning Host semantic snapshot, accept only a workspace-relative project source plus its exact SHA256 and UTF-16 caret offset, switch the existing paper workbench to that source, focus/reveal the caret, and never guess by PDF text search; text drag/double selection remains annotation, figure single/double actions remain annotation/open, stale/mismatched render or source revisions decline visibly without moving the caret, and no LLM/tool/skill invocation participates.
V105: ∀ Science desktop boot → register the mutation-derived deliverables accumulator before the first asynchronous Remote mount boundary, so initial history replay and an immediately started Turn cannot miss their `turn/start`; settled inline-code file mentions remain inert unless that synchronously registered accumulator proves the file was produced, and no prebuilt-frontend rewrite or legacy Markdown-link compatibility path is mounted.
V106: ∀ PDF text-layer interaction → harvest text selection on `pointerup`, but resolve inverse search only from the subsequent primary `click` MouseEvent with `detail=1`; never infer click count from `PointerEvent.detail`, while non-collapsed selection and double click remain annotation-only.
V107: ∀ product-owned native semantic writing boundary → name the executable, build target, Host process controller, and configuration `writing-preview-runtime`, carry an explicit bounded engine discriminator (`typst`), and keep engine-specific compilation behind that boundary.
V108: ∀ staged macOS writing preview executable → ad-hoc sign the final package path after copying the Cargo artifact, then verify it by executing the packaged-path integration contract; a valid linker signature on the source artifact alone ≠ runnable staged product.
V109: ∀ completed Turn containing safe workspace-relative `.typ|.typst` reference in closing Assistant prose or Tool arguments → history replay recovers one ordered deduplicated `Files` entry; inline-code mention and tail entry share one Session workbench opener; absolute/traversal candidates ⊥; generic `Inspect <tool>` action ⊥.
V110: ∀ active `science-typst` entry path A→B → remount paper-local editor state under B identity before rendering source/PDF; A draft, imported-source caret, save/conflict state, and inverse-search request ⊥ leak into B.
V111: ∀ `dsh-science` scientific publication-discovery request → the preset-scoped Science contract identifies `literature_search` as the local-Zotero-first tool and distinguishes it from `science_query`; ∀ `literature_search` → query only the running loopback Zotero v3 API, cap query/candidate/result/response sizes and duration, forward cancellation, reject non-loopback sources, omit attachment/full-text/path fields, and never fall back to a cloud provider implicitly; unavailable/disabled Zotero is an actionable error, not an empty successful search.
V112: ∀ Zotero candidate → emit one sanitized BibTeX file before provider metadata reaches ranking or Tool output; the owner-only snapshot carries stable citation key plus source/item identity, SHA256, source version, and entry count, while returned records include their bounded portable BibTeX entry and expose no snapshot host path.
V113: ∀ local literature query → return deterministic deduplicated relevance order with explicit `zotero-local-v1` ranking, structured title/authors/year/venue/DOI/URL/abstract/keywords, Zotero item key distinct from citation key, and ≤20 results; search appends no Science Journal fact, and only an explicit later evidence/import action may persist a selected citation in a paper or operational research fact.
V114: ∀ emitted Research Object → `@context=https://w3id.org/ro/crate/1.3/context`; flat `@graph` contains unique `@id`/`@type` entities, `ro-crate-metadata.json:CreativeWork` descriptor, and one project `Dataset` root with `name|description|datePublished|license|hasPart`; every graph edge uses `{ "@id": "..." }` references.
V115: ∀ Science entity projection → reuse Schema.org/RO-Crate terms: project→`Dataset`, question→`Question`, claim→`Claim`, evidence assessment→`Review`+`Rating`, experiment→`HowTo`, run/mutation→`CreateAction|UpdateAction`, artifact/document/figure/notebook→matching `CreativeWork|MediaObject|DigitalDocument|ImageObject|SoftwareSourceCode`; exposed vocabulary contains no ad-hoc short key.
V116: ∀ provenance exposed outside Host → represent derivation/execution through Action `object|result|instrument|actionStatus|startTime|endTime`; private Journal rows, custom `fromId|toId|type` relations, Session ids, and environment values ⊥ cross the RO-Crate boundary.
V117: ∀ persisted pre-RO-Crate `dsh-science-project@1` export event → replay remains readable and immutable; every newly requested export and Research Object query emits only RO-Crate 1.3.
V118: ∀ Research Object limit breach or invalid required entity/reference → fail the whole read/export with an actionable error; a truncated or partially linked graph ⊥ be labeled RO-Crate.
V119: ∀ new project export result → identify `filename=ro-crate-metadata.json`, `mediaType=application/ld+json`, and `format=ro-crate@1.3`; its parsed `content` must equal `getResearchObject` for the same immutable pre-export project state.
V120: ∀ renderer clipboard write → permit only `clipboard-sanitized-write` from the SwarmX window's main frame at the exact Harness origin; clipboard read, subframes, foreign origins/WebContents, and every unrelated permission remain denied.
V121: ∀ SwarmX desktop preset roster → expose `dsh-science` as read-only system content after DSH's shipped preset root and before the derived user root; metadata names it “科学模式”, and duplicate-id precedence remains configured-root order.
V122: ∀ agent preset ≠ `dsh-science` → Science tools, annotation/literature prompt sections, and Typst Bash guard ⊥ visible; ∀ agent preset = `dsh-science` → those registrations resolve through the preset layer and follow child agents, resume, fork, and cold transcript reconstruction, while `ctx.science`, Remote descriptors, browser artifact UI, and the generic Markdown deliverable-link prompt remain Host-global.
V123: `dsh-science/agent.cordis.yml` = the complete locked DSH `standard` composition followed only by product-owned Science rows; any DSH baseline update must fail a parity test until the product preset is deliberately rebased, with no private inheritance loader or compatibility path.
V124: ∀ Web display of system preset `dsh-science` → active `en` locale resolves `Science mode` plus its English description and active `zh` locale resolves `科学模式` plus its Chinese description through the same built-in preset locale path as DSH's shipped ids; roster metadata remains the fallback for unknown system and all user presets.
V125: `pnpm dev` = the canonical `pnpm start` chain by delegation; the development entry must not duplicate or diverge from the current-source build, Harness boot, and Electron launch commands.
V126: ∀ non-empty bounded selection wholly inside one rendered `user|steering|assistant-step` Chat row → show one keyboard-operable `Add to chat` action beside the selection; the first annotation inserts immediately, while a later selection opens a bounded optional-note editor beside the selection and Enter confirms; Tool, context, cross-row, streaming-only, or collapsed selections ⊥.
V127: ∀ `message_quote` annotation → carry the source `session_id`, durable message event `seq`, optional provider `message_id`, role, and exact selected text; destination Session identity remains an insertion argument rather than annotation state, so current-Session and future cross-Session references share one codec and persisted transcript projection.
V128: ∀ composer annotation set → contain ≤32 independently addressable detached occurrences without modifying visible draft text; the tray count equals the set size, its popup preserves insertion order and numbered selected text, hover/focus reveals working Edit and Remove actions, optional message-quote notes remain editable, and successful submit clears the same occurrence set while failed submit retains it.
V129: ∀ product-owned annotation UI copy → resolve from the `swarmx.annotation` locale namespace with complete `zh|en` dictionaries; English count copy distinguishes one/many and Chinese count copy uses one natural bounded template.
V130: ∀ committed PKB concept → UTF-8 OKF v0.2 Markdown under `$SWARMX_HOME/pkb/vault` is sole durable truth; any search cache/index is disposable and rebuildable from Markdown or the selected runtime's native conversation log.
V131: ∀ PKB concept → YAML frontmatter contains bounded `type|title|description|tags|status|generated|sources|swarmx_scope|swarmx_workspace`; body uses standard Markdown relative links + named footnotes; unknown frontmatter survives SwarmX round-trip; canonical content uses no Wikilink or Obsidian block reference.
V132: ∀ PKB workspace operation → Host derives one salted opaque key from `realpath(current authorized workspace root)`; symlink aliases converge, same-basename directories remain distinct, missing/unverifiable source workspaces decline, and no absolute host path reaches Tool output or Markdown.
V133: ∀ PKB read/search → expose global + current workspace concepts only; `search_conversations(scope=workspace)` reads only canonically matching native conversations from the selected runtime; `scope=all` requires one runtime-native single-use approval before any cross-workspace snippet enters model context.
V134: ∀ conversation-derived claim → `sources[].id` joins a body footnote to one bounded `Conversation Excerpt` page carrying one runtime-qualified native conversation locator and exact item/event seq bounds; expansion re-authorizes source scope against the current selected-runtime corpus and labels historic text untrusted evidence, never current instruction or permission.
V135: ∀ model PKB create/update/deprecate/conversation-capture → require one runtime-native single-use user decision for the exact carrier call (`ctx.approval allowed-once` on DSH, typed MCP elicitation on Codex); rejected/cancelled/unavailable/aborted action changes no concept, excerpt, index, log, or history file, including cancellation after a native conversation read but before the Vault writer lock commits; physical delete ⊥ model surface.
V136: ∀ PKB update/deprecate → require exact SHA256 revision, re-read immediately before commit, preserve prior bytes in owner-only revision history, then fsync + atomic replace; external Obsidian edits cause explicit conflict rather than silent stale overwrite.
V137: ∀ PKB create/update → path generated by Host under authorized scope, inputs/outputs bounded, directories `0700`, files `0600`, `index.md` and newest-first `log.md` regenerated deterministically, and failure never publishes a partial Markdown file.
V138: ∀ first DSH model request in one live Session → inject one deterministic frozen bounded snapshot of `global/index.md` + current workspace `index.md`; later PKB writes reach that Session through Tool results while a new/resumed process snapshots current files; Codex injects no synthetic Session prompt and accesses PKB explicitly through MCP calls.
V139: ∀ malformed hand-edited Markdown → preserve bytes, exclude that page from trusted synthesis, report actionable file-relative diagnostics, and never rewrite/quarantine/delete it implicitly; unknown OKF type or extension field alone ≠ malformed.
V140: ∀ PKB operation → local-only with no public route, implicit network, automatic sync, background LLM, or second durable transcript corpus; Science claims/experiments remain Science Journal entities and PKB may store only a locator or personal synthesis with explicit provenance.
V141: ∀ package-local test script reusing root Vitest include globs → set workspace root explicitly and constrain execution to that package's tests; filtered invocation discovers ≥1 test.
V142: ∀ product-owned workspace package relocation → package/test/docs references point to actual workspace directory; Host-only patch bare entries resolve from owning package manifest before boot, while client-bearing entries stay bare for DSH module registration.
V143: ∀ `ctx.gitUi.snapshot` → authorize from the live Session cwd, resolve configured Git lazily through `ctx.subprocess`, use explicit argv/cwd/stdio/env with no shell, bound stdout/stderr, forward abort, and return one typed `repository|not-repository|unavailable` result without preventing desktop boot.
V144: ∀ Git UI repository snapshot → require one non-bare repository with committed HEAD, run `--no-optional-locks status --porcelain=v2 -z --branch --untracked-files=all`, ignore unknown `#` headers, reject malformed records, cap entries, and expose only repository-relative changed paths plus branch/object identity; no canonical repository/cwd path crosses Remote.
V145: ∀ `dsh-ui-git` client → mount one read-only per-Session Version Control header action and keyed Details view whose Git Changes accordion is open by default with explicit refresh/error/empty/truncated states; register no model tool, staging, commit, branch, checkout, fetch, pull, push, or implicit polling mutation.
V146: ∀ `ctx.dvc.inspect` → require an existing DVC root contained by the inspected Git repository, resolve configured DVC lazily, perform no remote refresh/network/write, hash bounded regular `dvc.yaml|dvc.lock` bytes with SHA256, and expose only Git-relative DVC root plus deterministic category/count/digest summaries of bounded JSON status.
V147: ∀ DVC target/remote input → strict bounded strings reject option prefixes, NUL, absolute paths, and `..` path segments; remote names use one bounded identifier grammar; subprocess argv remains unambiguous with no shell concatenation.
V148: ∀ `ctx.dvc.pull` → only an explicit Host caller may mutate the addressed live workspace or contact its configured remote; the package registers no model tool/Remote endpoint, and settled output is bounded/path-redacted while cancellation terminates the complete process tree.
V149: ∀ `ctx.dvc.reproduce` → reject dirty/unborn/bare Git state, reproduce only an exact clean HEAD inside a package-private detached Git worktree, privately reuse source `.dvc/config.local` and cache without returning either path/content, optionally pull only when requested, preserve the disposable tree for caller inspection, and never mutate the source workspace.
V150: ∀ settled DVC command → distinguish executable/service failure from a started process's `succeeded|failed|cancelled` outcome; no nonzero stage result is mislabeled as successful reproduction, and every unreleased reproduction is removed on service disposal.
V151: ∀ Git/DVC workspace operation → serialize mutating operations per canonical repository, validate post-resolution containment before filesystem access, keep copied local configuration `0600` below the `0700` disposable owner, redact canonical paths/credential-bearing remote URLs from diagnostics, and never return credential material.
V152: ∀ desktop profile boot → mount exactly one `@swarmx/dsh-ui-git`, one `@swarmx/dsh-dvc`, and one `@swarmx/dsh-ui-dvc`; Git/DVC UI cross-process surfaces remain read-only, DVC mutation remains Host-only, and missing user CLIs become per-call actionable states/errors rather than boot failures.
V153: ∀ `ctx.dvcUi.snapshot` → authorize from the live Session cwd, call only `ctx.dvc.inspect`, validate the result at the Remote boundary, and map expected missing-project/CLI conditions to typed `not-project|unavailable` states without exposing canonical paths, remote URLs, credentials, command output, or mutation handles.
V154: ∀ `dsh-ui-dvc` client → mount only the strict DVC Remote contribution for the shared Version Control view and no separate header action or keyed Details view; register no model tool, initialize, add, pull, push, repro, experiment, implicit polling, or network action.
V155: ∀ DVC UI composition → mount `@swarmx/dsh-dvc` before `@swarmx/dsh-ui-dvc`, publish exactly one strict `dvcUi/snapshot` Typert endpoint, preserve `ctx.dvc` as the only DVC command owner, and keep optional CLI failure local to the rendered Session state.
V156: ∀ required real-DVC integration gate → initialize a temporary committed Git/DVC pipeline with the installed CLI, exercise `ctx.dvc.inspect` and exact-HEAD `ctx.dvc.reproduce`, prove source-workspace isolation and disposable-worktree cleanup, and fail explicitly when the CLI is required but unavailable.
V157: ∀ `dvc root` result → resolve the CLI's relative-or-absolute path against the command cwd, canonicalize it, then enforce Git-root containment before manifest or configuration access.
V158: ∀ test requiring multiple DSH loader-entry lookups or order comparison → materialize `ctx.loader.entries()` once before array operations; never cast and reuse its consumable iterator as an Array.
V159: ∀ real external-CLI integration test → declare a bounded timeout sized for full-suite contention; default unit-test timeout must not turn a successful DVC operation into a false failure.
V160: ∀ §B append → scan the complete section, allocate `max(existing id)+1`, and keep rows in strictly increasing numeric order; partial-tail reads must not determine identifiers.
V161: ∀ Version Control snapshot → Git remains independently usable; render one default-open DVC accordion iff `ctx.dvcUi.snapshot` returns `project`, hide `not-project|unavailable|failed` DVC detection without hiding Git, and display only path-free DVC identity plus data/pipeline category counts inside the shared panel.
V162: ∀ Swarm mutation → authorize the exact live `Agent`; Team id equals root Session id, member id equals continuable child Session id, and caller-provided names, `from`, owner, attempt, or UI Session id never grant authority.
V163: ∀ committed Swarm fact → append one bounded versioned event and update its materialized projection in one SQLite WAL transaction under owner-only `$SWARMX_HOME/swarm`; deterministic replay rebuilds projections, and workspace/Git/DVC contain no authoritative swarm file.
V164: ∀ member spawn → reserve one immutable lower-kebab name + child Session id in `provisioning` before `ctx.subagents.startContinuable`; publish `active|failed` afterward, reconcile crash-orphaned provisioning without duplicate members, inherit the root workspace/preset, and set delegation depth cap 0.
V165: ∀ Team task → bound text/count/dependency/write-scope inputs, reject missing blockers and cycles, increment exact revision on every transition, require expected revision plus active attempt for member completion/failure/release, and treat completed/cancelled rows as tombstones rather than reusable ids.
V166: ∀ Team-participant workspace-mutating Tool dispatch through a SwarmX-registered DSH or Codex product MCP Tool → agent-scoped guard requires that exact Agent's active `write` task/attempt; the lead has no ambient mutation exemption and may receive only explicitly lead-assigned work, one Team owns ≤1 write attempt, read tasks may overlap, attempt rotation waits for interrupted owner quiescence, and advisory write scopes never claim filesystem isolation or rollback; Codex-native shell/file Tools remain App Server-owned, do not cross this guard, and therefore make Swarm roles a coordination contract rather than a security boundary for native Tool execution.
V167: ∀ peer message → atomically queue bounded content under one stable id before delivery, including idempotency comparison, pending limit, and target-local sequence, then separately grant one atomic cross-process delivery claim; distinguish quiet injection from waking follow-up, retain undelivered mail for recovery, let only the exact target carrier recover its Codex mailbox, and expose neither body nor Session ids through UI projection.
V168: ∀ cold process recovery → every previously `in_progress` task becomes `needs_attention` with its attempt revoked; no non-idempotent work auto-replays. ∀ plugin disposal/reload → close admission, abort/release waiters, settle admitted scheduler/mailbox operations within a bound, revoke child setup, and close SQLite without detached work.
V169: ∀ `swarm/uiSnapshot|waitUi` → derive membership from one live Session, validate strict bounded payloads, return only that Team's path-free roster/task/count projection, use revision-based bounded long-polling, and reveal no workspace title/path, message body, model-private text, or raw Session id.
V170: ∀ `dsh-swarm` Agent surface → equal the exact locked `dsh-science` composition plus one preset-scoped aggregate `swarm` Tool/guidance; standard and `dsh-science` expose no `swarm`, members cannot use nested delegation or PKB, and all administrative actions remain lead-only.
V171: ∀ Swarm Client mount → reuse the generic per-Session Side View with one header action, keyed activity content, and bounded Tool-result card; register no overlay, peer conversation view, polling disk scan, direct mutation control, or independent layout geometry; HMR/session switch cancels every outstanding wait.
V172: ∀ Swarm/Science/PKB boundary → Swarm journal records orchestration ids and optional client-safe Science locators only; Science Journal remains scientific truth, Team members cannot mutate/read PKB directly, PKB receives no automatic Team synthesis, and RO-Crate receives no private Team/Session identity.
V173: ∀ Team retirement → model/user action is named `archive`; one replayable transactional archive fence first closes member admission, activation, scheduling, mailbox delivery, and ordinary mutation across every carrier, while already-started native provisioning must durably publish and clean its exact handle or explicitly settle no-handle before finalization; native stop acknowledgement and exact binding release become one durable member-retired acknowledgement; only after all live members, non-terminal attempts, and started intents drain may one durable archived edge commit, preserving history read-only and never claiming physical deletion.
V174: ∀ Team task → classify exactly one `read|write|knowledge` (R/W/K); R and K candidate analysis may overlap, W remains the sole effect lane, and ordinary `update_task(complete)` cannot complete K.
V175: ∀ admitted Team-participant W Tool body on a V166-guarded carrier → atomically append one effect intent keyed by current task attempt + carrier-stable call id before dispatch; duplicate call id, stale attempt, or another unresolved uncertain effect cannot enter the body; success settles the intent while error/timeout/cancellation/crash is conservatively uncertain.
V176: ∀ uncertain W effect → no automatic replay and no later W dispatch for that task until the exact lead records one typed verification as `observed|absent`; resolution must match the task's exact current revision and the effect's recorded attempt, and the contract suppresses duplicate dispatch without claiming opaque external exactly-once behavior.
V177: ∀ K admission → exact lead + current K revision/attempt + task-bound stable UUID + non-empty typed source locators + verified method/time precede any owner call; Swarm stores bounded admission audit/receipt only, never proposal text as truth, and only an idempotent owner receipt whose kind matches the requested Science Journal or approved PKB Vault target completes K; missing/mismatched post-call receipts remain uncertain.
V178: ∀ Science evidence admission → call existing workspace-authorized `linkEvidence` with the admission UUID as request id and only Science entity sources; ∀ PKB concept admission → obtain `allowed-once`, call the existing Vault with the same request id, and recover an identical owner concept instead of duplicating it; UUID reuse with changed content fails closed.
V179: ∀ cold recovery → revoke every active R/W/K attempt, convert started effect/admission intents to uncertain, and perform zero Tool/Science/PKB replay; authority safety, stale-attempt noninterference, crash-closed recovery, and epistemic noninterference must hold in the executable state model.
V180: ∀ repeated or concurrent quiet/wakeup submission carrying one idempotency key → accept at most one matching message, grant at most one native delivery attempt across processes, and return its current status; key reuse with different target, delivery, sender, or content is rejected.
V181: ∀ Team preflight evaluation → deterministically report cognitive DAG width, W scope-conflict rate, and effectful Tool density; recommend >1 member only for width ≥2, cap members by that width, count W as ≤1 serial lane, and never create or expand a Team automatically.
V182: ∀ fault benchmark → compare enforced and prompt-only models across crash phases, stale owners, uncertain Tool outcomes, duplicate delivery, recovery, and unadmitted knowledge; report safety-violation rate, duplicate effects, recovery replay, knowledge pollution, and coordination-write overhead without presenting bounded exploration as a universal proof.
V183: ∀ local Science resource address → canonical typed `sx:<prefix>/<encoded-id>`; exact `@revision` asserts current revision, rejects mismatch, and never requests history or falls back to latest.
V184: ∀ Resource Resolver call → authorize live Session workspace before snapshot lookup; typed collection index returns only current-workspace refs, rejects malformed/not-found/kind-conflict distinctly, and exposes no host path, Session/private environment, UUID-only model input, or Journal payload.
V185: ∀ resource view → `head|batch_head|neighbors` return refs + bounded summaries/edges, `get` returns explicit bounded projection, and Artifact `select` slices only existing digest-verified ≤64 KiB/500-row preview with ≤100 rows or ≤16 KiB text; truncation/unavailable state remains explicit.
V186: ∀ `science_query` action → action-specific strict Tool schema; `head|batch_head|get|select|neighbors` return `fact` + canonical/exact refs while `research_object|inspect_annotation` behavior and locator contract remain compatible.
V187: ∀ Team member → immutable `lead|legacy|researcher|implementer|monitor|verifier` role + bounded secret-free model-policy/budget snapshot; explicit child `agentOptions.provider|model|maxTokens` reach initial `startContinuable`, cold resume re-applies journal policy through Agent-scoped request composition, and missing legacy configuration preserves deployment defaults without guessing historical route.
V188: ∀ role-restricted Swarm action or SwarmX-registered Tool dispatch → exact live Agent remains sole authority; monitor cannot invoke guarded workspace mutation or mutate roster/task/budget, verifier cannot self-accept another member identity or invoke guarded workspace mutation, implementer cannot administer Team, role/name/provider/model strings grant zero authority, and Codex-native Tool execution remains subject to the explicit V166 boundary.
V189: ∀ task attempt → immutable member/role/model/budget snapshot + started/progress/submitted/verified/ended times + attributable usage accumulator; `maxWallMs` is Host-enforced hard deadline, token/turn ceilings are observed soft limits that may overshoot one request then prevent further work, unknown provider usage remains `unknown`, and warning/exhaustion edges deduplicate per attempt.
V190: ∀ non-K implementation settlement → exact owner + current revision/attempt may submit bounded summary/artifact/evidence only; submission revokes W authority; exact assigned verifier or lead + current submission may record structured verdict; only `pass` completes, `fail` rejects, `uncertain|escalate` enters lead review, self-verifying lead is explicitly `degraded`, and stale attempt/submission/verdict is inert.
V191: ∀ deterministic monitor evaluation → bounded stable finding over journal/Agent/task/usage facts only, no LLM or shell bypass; detect wall/stall/write/mailbox/lifecycle/role/submission/usage signals, deduplicate findings, use existing cancel/interrupt path for block actions, and never auto-replay non-idempotent work.
V192: ∀ attempt-economics event/projection → append-only transaction records only bounded model snapshot, aggregate usage/timing/tool count, client-safe artifact locators/digests, verdict, finding, terminal/escalation reason; ⊥ prompt, assistant response, message body, shell output, credential, environment, absolute path, raw provider payload.
V193: ∀ Swarm UI projection → strict read-only bounded role/model/budget/submission/verdict/usage/escalation summary, no raw Session/attempt id, message/prompt/body/path/credential, complete `zh|en` locale contract, bounded history, and unchanged shared-wait cancellation/accessibility/Side View geometry.
V194: ∀ Swarm v1/v2/v3/v4 database → serialized transactional idempotent v5 migration + deterministic event replay rebuild of Team/attempt/message-idempotency projections with explicit legacy defaults and earliest-key ownership, while exact runtime-member claims remain outside projection rebuild; legacy committed admissions without a valid matching owner receipt become uncertain without a fabricated receipt; newer version fails loud, active/submitted/verifying recovery becomes `needs_attention`, archive stays immutable/readable, owner-only `0700/0600` persists.
V195: ∀ optional semantic monitor → disabled by default, woken only by deduplicated submission/failure/stall/budget/lifecycle events, receives bounded de-identified summaries, returns strict non-executable verdict/finding, and has no workspace/guard/budget/verifier-policy mutation authority.
V196: ∀ shared runtime/orchestration/browser-protocol/domain authority → ⊥ DSH Agent/Session API, DSH Mux frame, Codex JSON-RPC method/type, runtime home path, or native Session/Thread implementation; platform names and types occur only below one `dsh` or `codex` adapter boundary.
V197: ∀ selected runtime → its native Session/Thread remains the only conversation truth; SwarmX stores no duplicate transcript and rebuilds one deterministic neutral projection from adapter `read` plus ordered events.
V198: ∀ runtime adapter → pass one shared conformance suite for list/create/read/start/steer/interrupt/revise/fork/archive/events/disposal; adapter-owned approvals pass the shared exact-request suite, while DSH-native approvals remain exclusively on DSH Web's existing provider; unsupported behavior fails explicitly, never through a fallback implementation.
V199: ∀ explicit Fork → create a native branch before the selected terminal turn; Codex uses exact `thread/fork.beforeTurnId`, including first-turn boundary, preserving native ancestry/configuration; ∀ Retry/Edit → submit through adapter `revise`, where a paginated Codex thread may replace only its latest terminal turn in place with `thread/revert` while DSH, legacy Codex history, and older Codex turns branch before the selected turn; Edit performs no native mutation until the revised draft is submitted.
V200: ∀ native event → adapter emits monotonically ordered runtime-qualified events; text deltas are provisional, completed native items are authoritative, duplicate/out-of-order frames are deduplicated or rejected, and reconnect/read converges to the same projection.
V201: ∀ approval request → bind selected runtime + conversation + turn + item/request identity; only one matching pending request may resolve it, cancellation/disposal rejects pending approval fail-closed, and renderer input cannot widen native authority.
V202: ∀ Codex adapter connection → spawn `codex app-server` with stdio, complete `initialize` exactly once before methods, validate every response/request/notification at the wire boundary, cap buffered JSONL/stderr, bound initialization plus root recovery by one finite startup deadline, and terminate the owned child plus pending calls on timeout or disposal.
V203: ∀ Codex startup → absent/`0` experimental-history setting selects legacy creation + branch revision; exact `1` selects `thread/start.historyMode=paginated` + latest-terminal-turn `thread/revert`; exact native branches use verified experimental `thread/fork.beforeTurnId`; user reads use bounded stable `thread/read(includeTurns=true)`, exact unmaterialized-before-first-message retries metadata-only and projects zero turns, incompatible allowlisted experimental behavior fails actionable; ⊥ capability guessing from published schemas, direct `thread/turns/list`, WebSocket, dynamic tools, unallowlisted experimental method, post-method-error fallback, or silent emulation.
V204: ∀ DSH adapter conversation → logical reads come from `ctx.sessionQuery`, live/resumed execution from `ctx.agents`, and branching from a completed stable DSH event boundary; native values are projected once at the adapter edge.
V205: ∀ `WorkspaceScope` → Host derives a canonical absolute root from selected conversation metadata or explicit user selection, mints an opaque token, rejects traversal/symlink escape and model-provided roots, and passes only that scope into domain services/tools.
V206: ∀ Science/PKB/Swarm core operation → accepts platform-neutral scope/actor/abort inputs; DSH services and Codex MCP handlers are thin validation/carrier adapters over the same implementation and produce the same bounded result.
V207: ∀ Codex product Tool → configured through the owned SwarmX MCP stdio server with exact `WorkspaceScope`; schema/result matches the DSH Tool handler and tool subprocess disposal cannot outlive its App Server/workspace owner.
V208: ∀ Swarm member runtime → neutral member handle owns create/followup/steer/interrupt/status/usage/dispose; DSH Agent and Codex child Thread implementations preserve coordinator one-W-attempt admission, exact-owner, budget, evidence, and recovery invariants within SwarmX-owned boundaries, with Codex-native Tool execution following V166.
V209: ∀ product state startup → resolve owner-only `$SWARMX_HOME`; if and only if the destination is uninitialized and legacy `$DSH_HOME/{science,pkb,swarm}` exists, perform one bounded idempotent copy-and-verify import with a durable marker, never dual-write or fall back afterward.
V210: ∀ desktop startup → boot the DSH Web profile as the sole UI/plugin host, register DSH plus the explicitly selected available peer adapter in one `conversationRuntimes` Host service, and load only the DSH Web URL; missing selected executable/configuration fails actionable and never silently switches runtime.
V211: ∀ conversation-runtime request/SSE connection → travel through the existing DSH `webServer` origin under `/api/swarmx/conversation-runtimes`, use strict bounded schemas and disconnect cancellation, leak no host path/native wire payload, serve no HTML/assets, and preserve Electron navigation/permission fences.
V212: ∀ DSH and Codex acceptance run → create, converse, stream, steer, interrupt, Retry, Edit, first/later Fork, resume after restart, approve/reject, invoke shared Science/PKB/Swarm Tools, and dispose with zero orphan child/process/pending request; Codex additionally verifies paginated read and same-thread last-turn revert without claiming workspace rollback.
V213: ∀ Codex conversation exposure → remain a native Codex Thread addressed by `(codex, nativeConversationId)`; ⊥ synthetic DSH Session, DSH API/Mux compatibility server, DSH Agent loop, duplicate transcript, browser app-server WebSocket.
V214: ∀ DSH Web runtime seam → Host extensibility uses an ordinary Cordis service plus `webServer.register`; browser changes consume only the narrow runtime protocol and never emulate workspace/subagent/projection/queue/command/attachment Session internals.
V215: ∀ MCP operation → domain Tool/Science/PKB/Swarm capability only; ⊥ list/create/read/start/steer/interrupt/fork/archive/subscribe/approval conversation lifecycle.
V216: ∀ Codex `turn/start` on adapter-unknown loaded state → call `thread/resume` exactly once before starting; `thread/read` never implies loaded/subscribed state; create/fork/resume marks loaded, `thread/closed|thread/archived|status:notLoaded` clears it, and resume failure remains actionable without duplicate turn submission.
V217: ∀ Codex list → follow unique `nextCursor` pages newest-first through explicit ≤10 page and ≤1000 summary bounds and include process-local unmaterialized created threads without copying transcript; successful first `turn/start` removes the transient entry; an unsent blank thread is intentionally non-durable across App Server restart.
V218: ∀ Codex `serverRequest/resolved` → remove/reject only the exact mapped pending native approval and emit one runtime-qualified `approval_resolved` event so browser state cannot retain a server-cleared prompt; present only supported string decisions supplied by `availableDecisions`, otherwise use the method's stable choices.
V219: ∀ Codex branch/archive → `thread/fork.beforeTurnId` directly excludes the selected turn and preserves native lineage for first/later boundaries, with no fallback on rejection; `thread/archive` retains native descendant-cascade semantics, the bounded archived `thread/list` is authoritative over an omitted or stale-false `thread/read.archived`, and every `thread/archived` notification clears that thread's loaded/transient/approval state.
V220: ∀ Codex persisted-history projection → expose only native persisted items; never fabricate missing command/tool history; oversized full-history JSONL or unsupported pagination fails actionable until App Server supplies stable item/turn pagination.
V221: ∀ PKB `create_knowledge` → normalize and validate request before approval; omitted `scope` becomes `workspace` on DSH and Codex MCP, explicit `global|workspace` survives unchanged, and normalized scope participates in one identical idempotency digest and carrier schema semantics.
V222: ∀ Host runtime SSE → registry assigns one globally monotonic `seq` exactly once before fan-out across all adapters/subscribers; completed items remain authoritative in Host and browser projections, so later/duplicate deltas cannot make them provisional or alter final content.
V223: ∀ Codex typed elicitation → accept only explicit MCP `mode=form` with the strict supported and internally self-consistent form-schema subset, reject every unknown keyword, invalid option/default/bound, or ambiguous identity tuple at ingress, project field type/required/options/bounds without string coercion, validate submitted content against that exact accepted schema before native resolution, and reject URL/missing/ill-typed content while preserving exact approval identity/cleanup.
V224: ∀ Codex product MCP Tool → publish the owning operation's complete strict MCP input schema while DSH receives only the structurally equivalent subset accepted by its Tool-schema dialect; both carrier projections preserve action/request coupling and defaults, share the same strict pre-invocation validator, and are never derived by reserializing one carrier's schema through another schema library; project the exact Codex-supplied native Thread identity only after the bridge verifies that Thread belongs to the current canonical workspace, otherwise use the exact stable transport session or opaque request identity; native child Thread bindings survive MCP restart in a cross-process transactional per-member store and resolve back to the reserved member authority; only the root product-lifetime Swarm owner may cold/final recover workspace attempts/effects/admissions or reconcile orphaned bindings, while every auxiliary per-Thread MCP process opens existing storage without migration/projection rebuild, may hydrate/synchronize only a currently claimed exact member or a lead's current Team handles, recovers mailbox content only for its exact caller, and never crash-recovers the journal, fails provisioning, archives sibling Threads, rewrites a workspace-wide binding snapshot, treats a revoked binding as lead authority, or trusts a cached actor after its claim/member phase changed; every synchronized native handle must preserve exact Thread/workspace identity and remain unarchived, otherwise fail/revoke that member before further scheduling; the root bridge lets native creation finish after an MCP caller disconnects, drains admitted creation/retirement for a bounded shutdown interval before App Server teardown, and transactionally claims the returned Thread for the exact still-provisioning member before publishing the response, so response loss leaves a recoverable claim; timeout/disconnect/unclassified rejection never proves absence, cleanup is single-flighted, exact source-tagged zero-turn provisioning Threads use native delete without fabricated history, materialized Threads use native archive, and exact claim release follows acknowledgement; the provisioning source tag is process-local correlation only because blank Threads are absent from lists and materialization clears the tag, so the owning bridge retains the exact pre-turn id and the transactional binding is the sole post-turn identity; root reconciliation promotes only a bound unarchived child with observed initial-turn materialization, retains the claim on cancellation/transient/protocol failure, and treats only a proven missing Thread—not a missing RPC method or missing tag—as absence; never invent an unavailable Thread or share one workspace-wide synthetic actor across unrelated calls.
V225: ∀ desktop profile boot → resolve DSH, product, and profile-installed bare entries from the active profile plus its maintained dependency-closure fallback; reconcile the shared fallback exactly to the current DSH installation by removing obsolete DSH-owned entries, and pass profile boot tests from both fresh and previously populated homes without incidental workspace hoisting.
V226: ∀ zero-generated-artifact checkout → `pnpm build` emits every required package entry in dependency order and succeeds without a prior typecheck/build or retained `lib|dist|target` output.
V227: ∀ selected runtime metadata/startup failure → surface one actionable fail-loud state and never retain/show DSH as a silent substitute; synchronous selection errors and asynchronous startup errors reach the same fatal boundary.
V228: `SWARMX_CODEX_FULL_ACCEPTANCE=1` → enable the real Codex V212 matrix rather than the smoke subset, including shared PKB mutation through MCP, typed approval, native Swarm child self-authority before and after MCP restart, lead archival of that child, stream/lifecycle operations, disposal, and zero identity-verified owned orphan processes.
V229: ∀ Science image result over DSH or Codex MCP → fully decode the accepted supported image, report its real intrinsic pixel dimensions and bytes, and fail malformed or MIME-mismatched data rather than publishing fabricated attachment metadata.
V230: ∀ runtime-owned asynchronous member delivery/interrupt/watch → journal success, terminal interruption, or idle only after native acknowledgement/observation, surface rejection without an unhandled Promise, retain authority when interruption is rejected, and fail/revoke a member when its native lifecycle can no longer be observed.
V231: ∀ desktop quit after startup or shutdown begins → quit waits for any in-flight platform boot, then attempts every disposer including final Swarm recovery; success continues normal quit, while startup/cleanup failure is reported once and exits non-zero instead of leaking a late-created platform or hanging behind a rejected Promise.
V232: ∀ desktop product-state lifetime → exactly one Electron primary instance owns `$SWARMX_HOME`, a non-configurable platform-level Swarm recovery owner, Harness, App Server, MCP children, and final recovery; the owner starts before configurable Harness plugins/App Server, stops after them, and DSH monitoring never scans a Team with no actor owned by that runtime; a secondary launch focuses the primary window and exits before booting any platform service.
V233: ∀ WikiSkill Raw read → resolve one exact DSH Session through `ctx.sessionQuery`, return a bounded contiguous `[startSeq,endSeq]` event window, preserve native event order/identity, honor cancellation, and persist only Session/seq locators; `$SWARMX_HOME/skills` and PKB contain no copied Raw trace or model-private reasoning corpus.
V234: ∀ WikiSkill Wiki fact → remain an ordinary current PKB `AgentPattern|SkillImpact` concept under V130–V140; existing workspace scope, provenance, approval, revision, history, index, and no-background-LLM behavior remain unchanged, and the skills layer may return a PKB-ready draft but never commits it implicitly.
V235: ∀ WikiSkill proposal → exactly one create/patch of one kebab-case skill, one bounded valid `SKILL.md`, one non-empty `PURPOSE.md`, one exact target `preset+model`, ≥1 DSH Raw locator, ≥1 exact PKB concept id+revision, and the expected active revision enter owner-only staging; staging is idempotent by proposal id+digest and is never a DSH skill root.
V236: ∀ WikiSkill resolution → same target/benchmark/task-set evaluates baseline and candidate with finite scores; accept iff candidate score is strictly greater, then recheck exact active revision and publish `PURPOSE.md` before atomically replacing the model-visible `SKILL.md`; reject/cancel/conflict changes no active skill or PKB concept, and every outcome returns a bounded `SkillImpact` draft with target, revisions, scores, verdict, and source locators.
V237: ∀ DSH WikiSkill runtime view → register only through the existing `ctx.skills` registry inside the exact Agent scope, select only `$SWARMX_HOME/skills/active/<target-key>` matching that Agent's current preset+model, invalidate on target change, and delegate discovery/body loading to `@deepseek-ai/dsh-skill-filesystem`; no staging path, `PURPOSE.md` body, provider path, or other target enters the catalog, and `@deepseek-ai/dsh-tool-skill` remains sole model-facing catalog/loader.
V238: ∀ WikiSkill storage mutation → strict boundary validation, owner-only directories/files, one cross-process writer lock, bounded input/output, cancellation recheck under lock, SHA256 proposal/active revisions, and no partial model-visible instruction replacement; unknown/malformed proposal or external active revision change fails closed.

## §T TASKS

id|status|task|cites
T49|x|document PKB contract + executable OKF fixtures|R17,R18,R19,R20,V130,V131,V134,V139,V140
T50|x|implement owner-only OKF Vault + revision/index/log lifecycle|I.`@swarmx/dsh-pkb`,I.`$SWARMX_HOME/pkb/vault`,V130,V131,V132,V136,V137,V139
T51|x|implement authorized cross-Session search + conversation evidence pages|R21,V132,V133,V134,V140
T52|x|register `ctx.pkb`, aggregate `pkb` tool, approval policy, and frozen index context|I.`ctx.pkb`,I.`pkb`,V133,V135,V138,V140
T53|x|compose desktop package + verify build, docs, security, lifecycle|V4,V15,V22,V32,V64,V84,V130,V140
T54|x|fix PKB relocation references + owner-anchored product patch resolution|V64,V141,V142
T55|x|document and test Git UI/DVC contracts|R22,R23,R24,R25,R26,V143,V144,V145,V146,V147,V148,V149,V150,V151,V152
T56|x|implement read-only `@swarmx/dsh-ui-git` Remote, header action, and Details view|I.`@swarmx/dsh-ui-git`,I.`ctx.gitUi.snapshot`,V143,V144,V145
T57|x|implement `@swarmx/dsh-dvc` status + explicit pull + isolated reproduction|I.`@swarmx/dsh-dvc`,I.`ctx.dvc.inspect`,I.`ctx.dvc.pull`,I.`ctx.dvc.reproduce`,V146,V147,V148,V149,V150,V151
T58|x|compose Git UI/DVC packages into desktop and verify build/docs/lifecycle|V4,V15,V22,V32,V141,V142,V152
T59|x|document and test DVC UI + real CLI contracts|I.`@swarmx/dsh-ui-dvc`,I.`ctx.dvcUi.snapshot`,V153,V154,V155,V156
T60|x|install DVC CLI and validate status/reproduction against a real temporary project|R24,R25,V146,V149,V150,V156
T61|x|implement read-only `@swarmx/dsh-ui-dvc` Remote, header action, and Details view|I.`@swarmx/dsh-ui-dvc`,I.`ctx.dvcUi.snapshot`,V153,V154,V155
T62|x|compose DVC UI into desktop and verify runtime/build/docs/lifecycle|V4,V15,V22,V32,V141,V142,V152,V155,V156
T63|x|merge Git and available DVC status into one accordion Version Control panel|I.`@swarmx/dsh-ui-git`,I.`@swarmx/dsh-ui-dvc`,V145,V154,V161
T64|x|document dsh-swarm contract + add failing journal/authority/DAG/mailbox/recovery tests|R27,R28,R29,V162,V163,V164,V165,V167,V168,V172,V173
T65|x|implement owner-only Swarm event journal, replayable projection, exact identity, task CAS, attempts, and bounded mailbox|I.`@swarmx/dsh-swarm`,I.`$SWARMX_HOME/swarm/swarm.sqlite`,I.`ctx.swarm`,V162,V163,V165,V167,V168,V173
T66|x|implement continuable members, scoped capability guard, event-driven single-writer scheduler, aggregate Tool, and fail-safe recovery|I.`ctx.swarm`,I.`swarm`,R29,V164,V166,V167,V168,V170,V172
T67|x|implement strict Swarm Remote, revision wait, header action, Side View activity, and bounded Tool card|I.`@swarmx/dsh-ui-swarm`,V169,V171
T68|x|ship `dsh-swarm` system preset and compose Host/UI packages into desktop profile|I.`dsh-swarm`,V14,V15,V32,V64,V142,V170,V171
T69|x|document operations and verify unit, lifecycle, client, preset, real-profile, build, lint, and code-map gates|V4,V15,V32,V141,V163,V168,V169,V170,V171
T70|x|define R/W/K, effect, evidence-admission, recovery, prior-art, and Team-sizing contracts|R30,R31,R32,R33,V174,V175,V176,V177,V178,V179,V180,V181,V182
T71|x|implement durable Tool-effect intents, duplicate message keys, and crash-closed reconciliation|I.`ctx.swarm`,I.`$SWARMX_HOME/swarm/swarm.sqlite`,V162,V166,V175,V176,V179,V180
T72|x|implement owner-preserving Science/PKB evidence admission and PKB create idempotency|I.`ctx.science`,I.`ctx.pkb`,V135,V140,V172,V177,V178
T73|x|add executable state exploration, fault benchmark, prompt-only control, and deterministic Team preflight|V174,V179,V181,V182
T74|x|ship workspace-scoped ID-addressed Science resource access|I.`ctx.science.headResource/batchHeadResources/getResource/selectResource/getResourceNeighbors`,V183,V184,V185,V186
T75|x|document heterogeneous routing, economics, verification, and monitor contract + add failing schemas/state/authority/UI tests|I.`ctx.swarm`,I.`swarm`,V187,V188,V189,V190,V193,V195
T76|x|implement v3 journal migration, replayable attempt ledger, usage aggregation, and deterministic monitor findings|I.`$SWARMX_HOME/swarm/swarm.sqlite`,V163,V168,V189,V191,V192,V194
T77|x|implement per-member routing, cold-resume policy, role guards, budgets, submit/verify/escalate actions, and semantic-monitor seam|I.`ctx.swarm`,I.`swarm`,R29,V162,V164,V166,V187,V188,V189,V190,V191,V195
T78|x|implement strict localized read-only role/economics/verdict Swarm Side View|I.`@swarmx/dsh-ui-swarm`,V169,V171,V193
T79|x|synchronize docs/code maps and verify related/full typecheck/lint/test/build/docs gates|V4,V15,V32,V163,V168,V170,V171,V187,V188,V189,V190,V191,V192,V193,V194,V195
T80|x|document the audited DSH Web seam and neutral runtime/workspace/tool contract; add failing adapter, Host protocol, projection, Retry/Edit/Fork, approval, and migration tests|R34,R35,R36,R37,V196,V197,V198,V199,V200,V201,V205,V209,V213,V214,V215
T81|x|implement platform-neutral conversation types, projection, controller, workspace scopes, product-home migration, runtime registry, and DSH-webServer API/SSE Cordis service|I.`ConversationRuntime`,I.`ConversationRuntimeRegistry`,I.`ctx.conversationRuntimes`,I.`ConversationController.retry/edit/fork`,I.`WorkspaceScope`,I.`/api/swarmx/conversation-runtimes/*`,I.`$SWARMX_HOME`,V196,V197,V199,V200,V205,V209,V211,V214
T82|x|implement minimal DSH runtime adapter and pass the shared conformance suite|I.`apps/desktop/src/runtime/dsh/*`,V198,V204,V210,V212
T83|x|implement validated Codex App Server stdio adapter, approvals, native Fork/Retry/Edit, lifecycle, and real-CLI integration tests|I.`apps/desktop/src/runtime/codex/*`,I.`codex app-server`,R34,R35,R36,V198,V200,V201,V202,V203,V210,V212
T84|x|extract Science, PKB, and Swarm platform-neutral services/handlers and retain thin DSH carriers|I.`WorkspaceScope`,V196,V205,V206,V208
T85|x|implement owned MCP stdio carrier for Codex Science/PKB/Swarm tools and parity tests against DSH handlers|I.`SwarmX MCP stdio server`,R37,V206,V207,V212
T86|x|connect the product conversation client inside DSH Web to the Host runtime protocol and delete the standalone runtime server/surface|I.`@swarmx/dsh-ui-conversation`,I.`/api/swarmx/conversation-runtimes/*`,V196,V197,V199,V200,V201,V211,V213,V214
T87|x|always boot DSH Web, wire explicit default runtime selection, build/package assets, actionable doctor errors, and product-home import|I.`pnpm start -- --runtime <dsh|codex>`,I.`$SWARMX_HOME`,V202,V209,V210,V213
T88|x|synchronize docs/code maps and verify unit, adapter-contract, real DSH, real Codex, typecheck, lint, test, build, docs, security, and orphan-process gates|V4,V15,V32,V196,V198,V202,V204,V206,V207,V208,V209,V210,V211,V212
T89|x|replace draft-time Codex Edit forking with submit-time runtime revision, paginated Codex history, same-thread last-turn revert, and explicit DSH/legacy/older-turn branching|I.`ConversationRuntime.revise`,I.`thread/revert`,V197,V199,V203,V212
T90|x|harden Codex resume, transient/list pagination, approval cleanup, exact native fork, cascade archive, docs, and real-CLI lifecycle gates|I.`apps/desktop/src/runtime/codex/*`,I.`thread/fork.beforeTurnId`,V197,V199,V201,V202,V203,V212,V216,V217,V218,V219,V220
T91|x|normalize PKB create scope before authorization/digest and prove DSH/Codex MCP carrier parity|I.`pkb`,I.`SwarmX MCP stdio server`,V206,V207,V221,V224
T92|x|harden global runtime event ordering, completed authority, typed elicitation, and exact browser approval submission|I.`ConversationRuntimeRegistry`,I.`/api/swarmx/conversation-runtimes/*`,V200,V201,V211,V222,V223
T93|x|repair clean profile dependency resolution, zero-artifact build ordering, and selected-runtime failure surfaces|I.`pnpm build`,I.`pnpm start -- --runtime <dsh\|codex>`,V210,V225,V226,V227
T94|x|synchronize docs/code maps and run focused, real-profile, full Codex, build, test, typecheck, lint, docs, diff, and orphan-process gates|V4,V15,V32,V212,V221,V222,V223,V224,V225,V226,V227,V228
T95|x|document DSH Raw + existing PKB Wiki + WikiSkill Skills contract and add failing store/raw/provider tests|I.`@swarmx/dsh-wikiskill`,I.`$SWARMX_HOME/skills`,R38,R39,V233,V234,V235,V236,V237,V238
T96|x|implement bounded DSH Raw reader, staged/active WikiSkill store, strict validation gate, rollback, and PKB-ready impact draft|I.`DshRawTraceReader.read`,I.`WikiSkillStore.stage/resolve/readProposal/readActive/activeRoot`,V233,V234,V235,V236,V238
T97|x|register exact-Agent target skill provider, wire DSH create/resume, and synchronize package/build/code maps|I.`registerWikiSkillProvider(agentCtx, store)`,V4,V14,V15,V32,V226,V237,V238

## §B BUGS

id|date|cause|fix
B51|2026-08-25|new package-local `vitest run` inherited root-relative include globs from package cwd and found no tests|package script pins root config
B52|2026-08-25|pinning root Vitest config did not change Vitest root, so root-relative globs still resolved from package cwd|V141
B53|2026-08-25|PKB relocation references misspelled `pkb` as `pkg`, breaking filtered tests, Host typecheck, layout checks, and code map|V142
B54|2026-08-25|product-owned PKB patch kept bare package entry against DSH module base, depending on incidental workspace hoisting|V142
B55|2026-08-25|owner-anchoring every product patch converted client-bearing Conversation/Science entries to file paths, removing their DSH client-module identity|V142
B56|2026-08-25|DVC 3.67 returned `dvc root` as `.`, but Host resolved it against its own process cwd instead of command cwd|V157
B57|2026-08-25|desktop test cast consumable `ctx.loader.entries()` iterator to Array, so first `find` closed it before DVC UI lookup|V158
B58|2026-08-25|real DVC integration finished alone but exceeded Vitest's 5s default under full-suite process contention|V159
B59|2026-08-25|new DVC UI test imports and real-CLI timeout edit were not Biome-formatted before verification|V4
B60|2026-08-25|backprop allocated IDs from a stale partial §B tail and duplicated existing `B54|B55`|V160
B61|2026-08-27|resource tests asserted nested matcher/string representation while query copy changed legacy annotation phrase casing|V186
B62|2026-08-27|resource prefix map inferred literal-only keys and Tool schema used bounds outside DSH enforced JSON subset|V186
B63|2026-08-27|`science_query` parameters combined root `type` + `oneOf`, which DSH schema validation rejects|V186
B64|2026-08-28|Codex `start` called `turn/start` after read/list without loading stored thread, so restart continuation returned `thread not found`|V216
B65|2026-08-28|Codex list read one 100-row page and omitted process-local unmaterialized new threads|V217
B66|2026-08-28|Codex ignored `serverRequest/resolved`, leaving server-cleared approvals pending in runtime/browser state|V218
B67|2026-08-28|first-turn Codex branch used `thread/start`, losing native fork lineage and source configuration|V219
B68|2026-08-28|Codex archive rejected approvals only for requested parent although native archive can cascade to descendant threads|V219
B69|2026-08-30|PKB create schema required `scope` although Tool prose promised a workspace default, so both carriers approved then rejected omitted scope before digest|V221
B70|2026-08-30|DSH and Codex adapters owned independent SSE counters and registry forwarded duplicate sequence numbers|V222
B71|2026-08-30|browser event projection appended a late delta to an authoritative completed item and marked it provisional again|V222
B72|2026-08-30|Codex elicitation projected every JSON field as text and returned unvalidated form content, while browser `accept` sent no form|V223
B73|2026-08-30|Codex MCP discarded DSH Tool schemas and collapsed unrelated calls onto one workspace-wide synthetic actor|V224
B74|2026-08-30|desktop loader used the app manifest as module base, bypassing the active profile's maintained dependency-closure fallback|V225
B75|2026-08-30|client build graph omitted PKB although workspace tsdown required its generated entry, so only retained Host type output made build pass|V226
B76|2026-08-30|peer metadata rejection silently left DSH visible and `whenReady().then` failed to catch synchronous selection errors|V227
B77|2026-08-30|real Codex smoke tests ignored `SWARMX_CODEX_FULL_ACCEPTANCE`, leaving V212 marked complete without its full opt-in matrix|V228
B78|2026-08-30|browser approval submission keyed prompt removal and typed answers by native approval id alone, so colliding requests could clear or reuse another turn's state|V223
B79|2026-08-30|root overview counted direct `literature_search` as an eighth aggregate Science Tool although the contract defines seven aggregate Tools plus that separate Tool|V4,V23
B80|2026-08-30|an unreferenced draft BibTeX export remained beside a fixed historical paper whose bibliography is authored inline, leaving an ownerless documentation artifact|V4
B81|2026-08-30|a pending Codex server-request handler wrote its late success or error reply after connection disposal, producing an unhandled closed-stdin rejection during cleanup|V228
B82|2026-08-30|the owned MCP process did not dispose its product hosts when App Server closed stdio, so App Server shutdown left an orphaned SwarmX MCP child|V228
B83|2026-08-30|two real Codex smoke cases inherited the operator's active `CODEX_HOME`, making acceptance contend with unrelated Desktop state and time out nondeterministically|V212,V228
B84|2026-08-30|the real DVC gate still exhausted its 30-second bound under the expanded full-suite process load although the same operation completed successfully alone|V159
B85|2026-08-30|a Codex peer could close stdin before a pending server-request handler settled, so its late reply still became an unhandled write failure before process exit|V228
B86|2026-08-30|the full Codex acceptance cleanup stopped at its first disposal failure, skipping later owned runtimes, processes, servers, and temporary roots|V228
B87|2026-08-30|product MCP host disposal stopped at its first rejected disposer and could leave later Swarm, Science, or Cordis resources alive|V207,V228
B88|2026-08-30|the MCP carrier reserialized owning Tool schemas through Zod, changing their published structure and narrowing otherwise unbounded integers|V224
B89|2026-08-30|PKB guidance generalized DSH-only approvals, Session locators, and frozen prompt injection to the Codex carrier despite its native Thread plus MCP boundary|V133,V134,V135,V138
B90|2026-08-30|Codex stdio Swarm used a fresh call actor even when Codex supplied exact native Thread metadata, making every created Team unmanageable after its create call|V207,V224
B91|2026-08-30|Codex runtime disposal sent SIGTERM but resolved before App Server exit and had no bounded SIGKILL escalation, so production teardown could outlive its owner|V207,V228
B92|2026-08-30|Codex quiet/root Swarm delivery discarded the asynchronous bridge Promise, allowing coordinator journals to claim delivery before native acceptance|V169,V220
B93|2026-08-30|Codex accepted an MCP form elicitation without `requestedSchema`, bypassing typed projection and strict response validation|V223
B94|2026-08-30|Codex thread listing bounded unique summaries but not advancing unique cursors, so empty or duplicate-only pages could continue forever|V217
B95|2026-08-30|a request inserted its pending Promise before a closed-stdin write threw synchronously, then lost that Promise and rejected it later without a handler|V228
B96|2026-08-30|Codex treated `mode=url` elicitation with an incidental schema as a browser form, allowing an unsupported flow through typed approval|V223
B97|2026-08-30|Codex Swarm keyed native child Threads only by reserved random member ids, so the child MCP call could not resolve its own member authority and the binding vanished on MCP restart|V224
B98|2026-08-30|Codex Swarm hashed lead authority from native Thread id alone and did not re-check the journal workspace, allowing the same Thread metadata on another workspace host to find a Team|V224
B99|2026-08-30|Codex Swarm fire-and-forgot native interrupt and watcher Promises, could claim idle after rejection, and closed its journal before admitted operations settled|V230
B100|2026-08-30|Codex Science returned hard-coded 1×1 attachment dimensions for every inline image, diverging from DSH and publishing false result metadata|V229
B101|2026-08-30|desktop `before-quit` handled only successful platform shutdown, so a rejected disposer left an unhandled rejection and permanently prevented exit|V231
B102|2026-08-30|full Codex cleanup used reusable numeric PIDs without process identity verification and could replace an earlier cleanup failure with a later one|V228
B103|2026-08-30|Codex connection treated every ChildProcess `error` as proof of exit, allowing a still-live process after a failed kill to bypass bounded SIGKILL escalation|V228
B104|2026-08-30|the SDK elicitation schema parser stripped unsupported JSON Schema keywords before validation, weakening the native form contract instead of rejecting it|V223
B105|2026-08-30|the hard-budget monitor recorded an attempt terminal before an asynchronous native interrupt acknowledged success|V230
B106|2026-08-30|the Codex Swarm carrier rejected durable actions even when MCP supplied an exact stable transport session identity|V224
B107|2026-08-30|the Codex image carrier accepted header-only PNG/GIF bytes and published dimensions without decoding a complete image|V229
B108|2026-08-30|Codex accepted missing elicitation mode, inconsistent defaults/options, and delimiter-colliding approval identity tuples|V223
B109|2026-08-30|a maximum-length runtime-qualified Codex Thread locator exceeded the PKB read/capture input bound|V134,V220
B110|2026-08-30|Codex member creation left a native child, actor, or persisted binding when initial delivery failed before returning the reserved member id|V224,V230
B111|2026-08-30|required MCP startup observed restored members before the production runtime bridge was attached and permanently failed them|V224,V228
B112|2026-08-30|Codex Swarm recovery and member rehydration traversed active Teams from every workspace in the shared journal|V168,V224
B113|2026-08-30|the MCP carrier trusted native Thread metadata without verifying that the Thread belonged to its fixed workspace|V205,V224
B114|2026-08-30|required boolean elicitation fields displayed false but could not submit that valid false until manually toggled twice|V223
B115|2026-08-30|partial product MCP startup did not dispose already-created Server, Swarm, Science, and Cordis resources after a later initialization failure|V207,V228
B116|2026-08-30|the full real Codex gate covered only lead Swarm calls and omitted native child self-authority, restart recovery, and archival|V228
B117|2026-08-30|Codex App Server started the required MCP process for a newly created child Thread before its private binding was committed, and eager MCP-constructor rehydration permanently failed that in-flight member|V164,V224,V228
B118|2026-08-30|each Codex per-Thread MCP process could crash-recover the live workspace on first use/disposal and replace a shared whole-workspace binding file from stale local actors, failing in-flight members and losing concurrent native Thread bindings|V224,V232
B119|2026-08-30|auxiliary Codex MCP construction still migrated and rebuilt every shared Swarm projection, mutating siblings and competing for the required-server startup write lock|V194,V224
B120|2026-08-30|cancelled Codex member startup released its durable binding before attempting archive with the already-aborted request signal, losing the only recoverable handle to an orphan native Thread|V224,V230
B121|2026-08-30|a revoked or failed Codex member binding could fall through to new lead authority, while another process could keep using a cached member actor after retirement or exact release|V224
B122|2026-08-30|root Codex reconciliation promoted archived or empty pre-ack child Threads and retained invalid active bindings without observing native materialization|V224,V230
B123|2026-08-30|the DSH monitor scanned Codex Teams in the shared journal and treated a missing DSH actor as successful cancellation before terminalizing the foreign attempt|V230,V232
B124|2026-08-30|desktop-wide recovery depended on a configurable Harness plugin that could be disabled, replaced, reloaded, or disposed while App Server still owned live work|V232
B125|2026-08-30|desktop quit during asynchronous platform startup observed no assigned platform and exited without awaiting or disposing the resources created moments later|V231,V232
B126|2026-08-30|a child-side scheduler journaled an active attempt before proving that its process owned the lead handle needed for native assignment delivery|V164,V168,V230
B127|2026-08-30|concurrent per-Thread MCP processes could race message sequence/idempotency checks and both enter native delivery for one durable message|V167,V180,V224
B128|2026-08-30|Codex member synchronization treated an externally archived or identity-mismatched native child as idle and left its authority and attempts active|V224,V230
B129|2026-08-30|root reconciliation guessed missing native Threads from error text, so cancellation or a missing `thread/read` RPC method could destructively release a valid durable claim|V224,V227,V230
B130|2026-08-30|failed Codex provisioning archived the handle returned by native create even when its binding claim proved that another member already owned that Thread|V164,V224,V230
B131|2026-08-30|an MCP timeout or lost bridge response after native child creation could leave an unclaimed Thread that root recovery could never identify or clean|V164,V224,V230,V232
B132|2026-08-30|message delivery could commit its irreversible intent after the cached target actor had been failed or revoked by another carrier|V167,V180,V224
B133|2026-08-30|a lifecycle failure could mark a member failed while a concurrent scheduler inserted a new active attempt after the only revocation pass|V168,V230
B134|2026-08-30|bounded mailbox projection trimming forgot old idempotency keys and target sequence, allowing a delivered request to execute again|V167,V180
B135|2026-08-30|concurrent archive, member admission, provisioning, and scheduling could publish an archived Team with a live child, active member, or non-terminal attempt|V164,V168,V173,V224,V230
B136|2026-08-30|a bound Codex Team participant could invoke Science or PKB directly through MCP without the shared role, write-attempt, and Tool-effect guards|V166,V170,V175,V207,V224
B137|2026-08-30|knowledge admission reuse could cross task identity and an owner receipt could mismatch its target or be absent in a legacy committed event|V177,V179,V194
B138|2026-08-30|PKB cancellation after approval could enter the Vault mutation because the signal was not rechecked under the file lock|V135,V140,V221
B139|2026-08-30|v5 replay applied newly strict admission receipt invariants to old valid v1-v4 events and could reject the entire database|V177,V179,V194
B140|2026-08-30|mailbox projection trimming and migration did not durably preserve earliest message-key ownership and target sequence across all historical rows|V167,V180,V194
B141|2026-08-30|member startup timeout or unclassified rejection was treated as proof of no native handle, while a zero-turn Codex Thread cannot use native archive|V164,V173,V224,V230,V232
B142|2026-08-30|concurrent Team archive callers could issue duplicate non-idempotent native archive requests for one member|V173,V224,V230
B143|2026-08-30|runtime bridge disposal could finish before a disconnected root creation/cleanup operation, or wait forever for a stuck App Server request|V224,V228,V232
B144|2026-08-30|Swarm guidance claimed Codex-native shell/file Tools crossed the DSH/product MCP attempt guard even though App Server executes them outside SwarmX|V166,V207,V213
B145|2026-08-30|the DSH Team lead classified the aggregate PKB Tool only by name, allowing mutating PKB actions to bypass the write-attempt and effect guards while Codex MCP denied them|V166,V175,V206
B146|2026-08-30|a product MCP stdio transport connection failure after Host construction reported the error without disposing the initialized Server, Swarm journal, or Science resources|V207,V228,V232
B147|2026-08-30|conversation capture dropped its abort signal after the native read, so both DSH and Codex carriers could publish an excerpt after cancellation|V135,V140,V221
B148|2026-08-30|launcher root overrides replaced the complete composed Science, PKB, and Swarm configs, silently discarding profile/home privacy limits and monitor policy|V87,V152,V210
B149|2026-08-30|opaque per-call Codex MCP identities accumulated permanent in-memory lead actors even though those calls cannot own durable Swarm authority|V207,V224
B150|2026-08-30|the DSH adapter listed archived Sessions and could resume/followup or fork them, while Codex hid archived Threads and rejected further mutation|V198,V204,V219
B151|2026-08-30|a platform boot rejection observed concurrently by startup and before-quit invoked the fatal reporter and application exit twice|V231
B152|2026-08-30|the Codex Science MCP carrier discarded composed privacy limits, metadata opt-outs, and runtime settings while the DSH carrier honored them|V87,V206,V207
B153|2026-08-30|completed-item and adapter-sequence enforcement lived only in an unused projection class, so the production Host/browser path could accept contradictory completions and regressing SSE frames|V200,V222
B154|2026-08-30|partial desktop startup swallowed disposer failures and reported only the original startup error even when owned resources might remain live|V231,V232
B155|2026-08-30|legacy product-state import enforced local subtree totals only after recursive copies, so nested and peer directories could exceed the whole-import entry/byte budget before rejection|V209
B156|2026-08-30|the published `science_query` schema decoupled action from request variants and omitted runtime bounds, accepting combinations that the carrier rejected|V206,V207
B157|2026-08-30|typed Codex elicitation rejected the MCP SDK's legal bounded `$schema` field even though all property schemas were supported|V201,V223
B158|2026-08-30|a spawned Codex App Server that never answered initialize left platform boot and quit waiting forever because startup had no deadline|V202,V227,V231
B159|2026-08-30|typed elicitation used prototype-sensitive property lookup and form assignment, so inherited or `__proto__` field names could bypass declaration checks or disappear before submission|V201,V223
B160|2026-08-30|the UI and DSH adapter allowed archiving a running Session without stopping it, hiding the only neutral interrupt path while native work could continue|V198,V211
B161|2026-08-30|the DSH adapter read titles for the complete Session corpus with unbounded concurrency and returned an unbounded conversation list|V198,V211
B162|2026-08-30|a native read during a running Codex turn marked partial assistant items durable and the browser refused their later deltas or running-tool completions|V197,V200,V222
B163|2026-08-30|the Science resource formatter could emit a percent-encoded canonical ID above the parser's 1024-character limit, breaking formatter/parser round-trip|V186,V206
B164|2026-08-30|DSH mutation and fork paths did not recheck cancellation after awaited native lookup/resume, allowing disconnected requests to cross the irreversible call boundary|V198,V211
B165|2026-08-30|Codex empty-thread fallback matched an error substring instead of the exact App Server RPC identity, reinterpreting unrelated read failures as empty Threads|V203,V227
B166|2026-08-30|Science carrier projection allowed configurations larger than the Codex MCP environment parser accepted, so a valid DSH configuration could fail only on Codex|V206,V207
B167|2026-08-30|Host request cancellation observed only request-body aborts, so browser disconnects after a complete body did not cancel reads or mutations|V211
B168|2026-08-30|conversation protocol disposal neither owned in-flight routes nor bounded their drain before adapter teardown, allowing late native work or a shutdown deadlock|V211,V231,V232
B169|2026-08-30|SSE fanout ignored response backpressure, allowing one connected slow renderer to accumulate an unbounded event buffer|V211
B170|2026-08-30|Codex startup timeout accepted values above Node's timer range, which Node clamps to 1ms and turns into an immediate false startup failure|V202,V227
B171|2026-08-30|the Science schema fix conflated DSH's restricted Tool dialect with MCP JSON Schema, leaving either DSH registration invalid or MCP bounds and action/request coupling incomplete|V206,V207,V224
B172|2026-08-30|the Codex startup deadline covered initialize only, so a silent recovery RPC could hang platform boot and quit forever after successful initialization|V202,V227,V231
B173|2026-08-30|a BrowserWindow creation failure after platform boot exited Electron without disposing the already-owned Harness, App Server, MCP, or recovery owner|V231,V232
B174|2026-08-30|real App Server `thread/read` reported `archived:false` after external archive, and the adapter trusted that stale field over the authoritative archived Thread list|V198,V219
B175|2026-08-30|the real disposal gate assumed a pending MCP call must reject at transport level and reused a Team lead's server after child provisioning, so an MCP error result caused an unawaited assertion and masked cleanup coverage|V228,V231,V232
B176|2026-08-31|the alpha.2 shared module-fallback healer verified required entries but retained obsolete DSH-owned packages from a previously populated home, leaving removed pre-alpha modules resolvable|V84,V225
