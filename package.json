{
  "name": "swarmx-monorepo",
  "private": true,
  "version": "3.2.0",
  "description": "SwarmX multi-agent orchestration — Electron + Node.js edition",
  "scripts": {
    "build": "pnpm -r build",
    "audit:prod": "pnpm audit --prod",
    "ci": "pnpm run ci:node && pnpm run ci:inspect",
    "ci:inspect": "uv sync --locked --group inspect && pnpm test:inspect",
    "ci:node": "pnpm docs:check && pnpm lint && pnpm test:coverage && pnpm -r build",
    "dev": "pnpm -r dev",
    "docs:check": "node scripts/check-codebase-docs.mjs",
    "eval:inspect": "uv run --locked --group inspect inspect eval evals/inspect/tasks.py@swarmx_smoke --model none",
    "test:python": "uv run --locked --no-default-groups python -m unittest discover -s tests/python -p '*_test.py'",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:inspect": "uv run --locked --group inspect python -m unittest discover -s evals/inspect -p '*_test.py' && pnpm eval:inspect",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@vitejs/plugin-react": "^6.0.5",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "@hono/node-server": "2.0.12",
    "body-parser": "2.3.0",
    "fast-uri": "3.1.4",
    "hono": "4.12.32",
    "lodash-es": "4.18.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
